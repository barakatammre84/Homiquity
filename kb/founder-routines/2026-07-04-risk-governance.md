# Risk & Model Governance — 2026-07-04

**STATUS: FAIL** — the live VA residual-income calculation contradicts its own cited source, and applies an uncited 5% "discount" nowhere in VA Pamphlet 26-7. First run of this routine; no prior report to diff against.

## ⛔ Human actions

1. **⛔ Decide what the VA path should actually do.** `server/underwritingEngine.ts` — the file `decisionEngine.ts` actually calls for every real decision — computes VA residual income with an **uncited 18% tax-withholding estimate** (line 438: `combinedGrossMonthlyIncome * 0.18 // Standard 18% tax deduction model`) and an **uncited 5% "Active-Duty Commissary Facility Discount"** (lines 462–470: `requiredResidualIncome * 0.95` when `isActiveDuty && hasExchangeAccess`). Neither appears in VA Pamphlet 26-7 Ch. 4, `kb/regulatory-ledger.json`, or `kb/UNDERWRITING_SCENARIOS.md` S-02. Meanwhile the *tested, cited* implementation (`server/services/underwritingNuance.ts` `computeVaResidualIncome`, `RESIDUAL_TAX_RATE = 0.22`) uses 22% and has no commissary discount at all — and is **not the code path that runs real decisions**. This is regulated math; per this routine's charter I can flag it but not fix it. Two questions need your (or a lending-ops) call: (a) is the 18%/0.95 pair a real, sourceable VA policy that the cited 22% figure and the scenario docs are simply missing? or (b) is `underwritingEngine.ts` carrying stale/invented logic that should be replaced with the cited 22%-and-no-discount version? Either way it needs a citation added to `kb/regulatory-ledger.json` and a fix routed through the guardian/scenario lane (S-02), never patched directly from a governance report.
2. No production intake pause is being recommended today — there is no live borrower traffic (pre-launch; this finding is in seed/simulated-data code review, not an active-harm incident). But no real VA loan should be decisioned until #1 is resolved. Runbook on file in case that changes: production intake pause = set `INTAKE_PAUSED=true` in Vercel env + redeploy (`server/services/maintenanceMode.ts`); rollback = `git revert <sha>` + push. Not executed — no env vars touched, no push to main.

## Summary

Sampled 5 hard-coded thresholds across the decision path this run (rotating: none were sampled before today, since this is the routine's first execution). Found one FAIL — a regulated VA residual-income number in the file that actually runs live decisions contradicts the cited/tested number in the reference implementation, plus a second, entirely uncited "discount" multiplier applied on top of it — and two smaller WARNs (an unused, uncited constant; a used-but-uncited 90-day window). The full underwriting regression suite (66 tests) and both audit-chain test files (15 tests) pass. The credit_audit_log hash chain verified clean read-only against all 16 dev-DB applications plus the MCP agent chain (4 entries). The `ensureAdverseActionForDenial` chokepoint is the sole path to a "denied" status — confirmed no bypass. The AI model inventory matches actual `server/`-side AI-SDK usage (Gemini in `extractionService.ts`, OpenAI in `coachingService.ts`) and both stay behaviorally advisory. Fraud/input-integrity posture has real coverage (low-confidence extraction routing, Zod at intake, simulated-data flags) but zero duplicate-application/identity-mismatch detection — a genuine, undocumented-until-now gap, though real fraud checks are F3/F4-gated on live vendor data regardless.

## Checks run → results → evidence

### 1. Threshold citation audit (sampled today: `INCOME_DISCREPANCY_THRESHOLD_PCT`, `NEW_TRADELINE_WINDOW_DAYS`, `RESIDUAL_TAX_RATE`/VA tax path, `RENTAL_INCOME_VACANCY_FACTOR`, `ASSUMED_ANNUAL_RATE`/`TAX_INSURANCE_ANNUAL_PCT` — next rotation should cover `SEASONING_FULL/CONDITIONAL_MONTHS`, `SIGNIFICANT_DEPOSIT_INCOME_FACTOR`, `DEFERRED_STUDENT_LOAN_FACTOR`, `CONVENTIONAL_DTI_CAP`/`CONVENTIONAL_STRETCH_DTI`/`CONVENTIONAL_FICO_FLOOR`/`CONFORMING_LOAN_LIMIT` DB-resolved policy scalars in `server/underwritingEngine.ts`)

| Constant | Location | Ledger/scenario citation | Result |
|---|---|---|---|
| VA residual tax rate | `underwritingEngine.ts:438` (`0.18`, live decision path) vs `underwritingNuance.ts:229` `RESIDUAL_TAX_RATE` (`0.22`, cited/tested but not the live path) | ledger `va-26-7-ch4-residual-matrix` cites VA Pamphlet 26-7 Ch.4 for the matrix/utility-rate/cushion, silent on tax rate; `tests/complianceInvariants.test.ts` only asserts citations exist in `underwritingNuance.ts`, never checks `underwritingEngine.ts` | **FAIL — contradicts its own citation.** Two different tax-withholding assumptions for the same regulated calculation, and the untested one is the one that runs. |
| Active-Duty Commissary Facility Discount (`underwritingEngine.ts:462-470`, ×0.95) | `underwritingEngine.ts` | none — not in ledger, not in `UNDERWRITING_SCENARIOS.md` S-02, no test references `isActiveDuty`/`hasExchangeAccess` | **FAIL — untraceable number actively loosening a live VA pass/fail bar.** Cite or remove. |
| `INCOME_DISCREPANCY_THRESHOLD_PCT = 5` | `underwritingNuance.ts:70` | not in ledger; `grep -rn` across the repo shows it is declared and exported but **never imported or called anywhere** | WARN — untraceable AND dead. Cite-and-wire into `assessIncomeSeasoning`'s discrepancy flag, or delete. |
| `NEW_TRADELINE_WINDOW_DAYS = 90` | `underwritingNuance.ts:88`, used live in `adjustLiabilities` (sleeper-debt detection) | S-03 cites B3-6-05 for the 1% deferred-student-loan factor only; the 90-day new-tradeline window itself is uncited anywhere | WARN — cite the window (this may be a legitimate B3-6-05/AUS convention) or document it as platform policy like the other uncited-but-labeled constants. |
| `RENTAL_INCOME_VACANCY_FACTOR = 0.75` | `underwritingNuance.ts:312` | S-05 in `UNDERWRITING_SCENARIOS.md`, Fannie B3-3.1-08 — inline comment matches | OK — traced. Included as a control case to confirm the audit method distinguishes cited from uncited correctly. |
| `ASSUMED_ANNUAL_RATE = 0.07` / `TAX_INSURANCE_ANNUAL_PCT = 0.0125` | `preUnderwriting.ts:51-52` | Not a regulatory citation — explicitly commented as an advisory pre-rate-lock estimate matching the funnel's own math | OK — appropriately labeled as internal estimate, not represented as a sourced rule. No action. |

### 2. Scenario regression
`npx vitest run tests/underwritingNuance.test.ts tests/scenarioCatalog.test.ts tests/complianceInvariants.test.ts` → **3 files, 66 tests, all passed.** Standing flag (not news): S-05/S-06 rental-income offsets remain LO-facing advisory only — they never reach `decisionEngine`'s actual DTI. Note: this suite's citation-traceability tests (`complianceInvariants.test.ts` lines 84-91) only read `underwritingNuance.ts` — they gave a false sense of coverage for VA residual income, since the number that runs is in a different, unchecked file (see finding #1 above).

### 3. Model inventory vs reality
`grep -rl` for AI-SDK usage under `server/` found exactly two files importing a model SDK: `server/extractionService.ts` (`GoogleGenerativeAI` / `genAI.models.generateContent`, Gemini) and `server/services/coachingService.ts` (`import OpenAI from "openai"`). This matches `MODEL_RISK_GOVERNANCE.md` M-1 and M-5. No new model IDs, no new prompt surfaces, no AI usage outside the inventory. Behavioral boundary check: `coachingService.ts` carries an explicit compliance footer ("does not constitute a lending decision..."), instructs the model never to say "approved"/"eligible," and its Zod-validated outputs (`IntakeSchema`/`ActionPlanSchema`/`DocumentChecklistSchema`) reject malformed model output rather than passing it through. `documentConfidence.ts` routes low-confidence extractions (`< 0.7`) to human review; it does not itself call a model. See Corrections table — this routine's own file list (`aiGateway.ts`, `documentConfidence.ts`) doesn't match the actual code.

### 4. Decision audit trail
Ran the existing `verifyAuditLogIntegrity` / `verifyAgentAuditLogIntegrity` functions (`server/services/creditService.ts`) read-only against the dev DB via a temporary script (written, executed, deleted — not committed): **16/16 per-application chains valid**, entry counts 1–7; **agent (null-application) chain valid**, 4 entries. Test coverage: `npx vitest run tests/mcpAudit.test.ts tests/auditReanchor.test.ts` → 2 files, 15 tests, all passed. Chokepoint check: `grep -rn "denied"` across routes/services shows exactly one place sets `status = "denied"` (`server/routes/lending.ts:1333/1402`), which calls `ensureAdverseActionForDenial` before persisting; the separate staff compliance route (`server/routes/compliance.ts:866`) calls `creditService.generateAdverseAction` directly for manually-generated notices, which is the same audited function `ensureAdverseActionForDenial` wraps. No bypass found.

### 5. Fraud & input-integrity posture
- **Exists:** low-confidence document routing (`documentConfidence.ts`, `< 0.7` → `DOCUMENT_OCR_ISSUE`-style human review); Zod validation on intake routes (7 `parse`/`safeParse`/`z.object` call sites in `lending.ts`); simulated-vendor trust-boundary flags (`creditService.ts` `isSimulated`/`simulated: true` on every soft-pull row; `simulateCreditPullCompletion` hard-throws in production without `CREDIT_VENDOR_MODE=simulation`).
- **Missing (ticketed, not fabricated):** `grep -rn "duplicate.*application|identity.*mismatch"` across `server/` returned nothing — there is no duplicate-application or identity-mismatch detection anywhere in the codebase today.
- **Real fraud coverage on live vendor data remains ⛔ F3/F4-gated** — the simulated soft-pull has no collection tradelines or cross-application identity signal to check against; this is unchanged from prior audits, said plainly rather than inventing coverage. Plaid (`server/plaid.ts`) is a real sandbox integration (not simulated data) gated on `PLAID_CLIENT_ID`/`PLAID_SECRET` presence.

## Corrections table

| Doctrine/prompt said | Verified reality |
|---|---|
| Task prompt: "the only allowed AI-SDK files are `server/extractionService.ts`, `server/services/coachingService.ts`, `server/services/aiGateway.ts`, `server/services/documentConfidence.ts`" | `server/services/aiGateway.ts` **does not exist** in the repo. `server/services/documentConfidence.ts` imports no AI SDK — it only consumes confidence scores/analytics. The actual two AI-SDK-importing files are `extractionService.ts` (Gemini) and `coachingService.ts` (OpenAI), matching `MODEL_RISK_GOVERNANCE.md` M-1/M-5. Update this routine's file list to match the authoritative inventory doc rather than a hardcoded list. |
| Implicit assumption (ledger + S-02) that `underwritingNuance.ts`'s `computeVaResidualIncome` is what actually decides VA files | It is tested and cited, but `decisionEngine.ts` calls `consolidatedUnderwritingEngine.evaluate()` (`server/underwritingEngine.ts`), which has its **own separate, inline** VA residual math with a different tax rate and an extra uncited discount. Two implementations of the same regulated calculation exist; only one is exercised by tests. |
| No prior risk-governance report existed to rotate against | Confirmed — this is the inaugural run of this routine; `kb/founder-routines/` did exist already (created by today's security/vendor-procurement routines) but had no risk-governance file until now. |

## Remediation tickets

- **P0 (added to CTO_ROADMAP.md #29, this commit) — VA residual-income tax rate contradicts its citation + uncited discount actively live.** Owner: Claude (draft the fix), but execution must go through the guardian/scenario lane per this routine's charter — not shipped from a governance report. Human decision needed first (see Human action #1). Est. 3h once the source-of-truth is confirmed (align `underwritingEngine.ts` to the cited 22% figure or update the citation/tests to match 18%+discount, whichever is verified correct; add ledger entry either way).
- **P1 — `INCOME_DISCREPANCY_THRESHOLD_PCT` is declared, uncited, and never used anywhere.** Owner: Claude. Est. 1h — either wire it into `assessIncomeSeasoning`'s discrepancy check with a citation, or delete the dead constant.
- **P1 — `NEW_TRADELINE_WINDOW_DAYS = 90` is live (sleeper-debt detection) but uncited.** Owner: Claude. Est. 1h — research whether B3-6-05/AUS convention specifies this window; add a `regulatory-ledger.json` entry or relabel as platform policy like `platform-reserves-2-months`.
- **P2 — No duplicate-application / identity-mismatch detection exists.** Owner: Amr (decide what "duplicate" means across statuses/withdrawn apps — a business/policy call) + Claude (implementation once scoped). Est. 4h. Real teeth blocked on F3 live credit-data anyway; worth designing the check now so it's ready to wire in.

Roadmap ticket #29 appended to `CTO_ROADMAP.md` "Do next" in this commit (grepped first — no existing entry for the VA residual contradiction).

---
STATUS: FAIL
