# Evening Triage — Chief of Staff & Finance/GTM — 2026-07-04

**STATUS: FAIL** — driven by the 10:07a Risk & Model Governance routine's FAIL (live VA residual-income math contradicts its own citation, roadmap #29, already ticketed); compounded by 6 of today's 13 executive routines never producing a report. No production intake pause is warranted — this is pre-launch, no live borrower traffic, and the FAIL is a code-correctness finding, not an active incident.

## ⛔ Human actions (hardest first)

1. **⛔ Decide the VA residual-income source of truth (roadmap #29, risk-governance FAIL).** `server/underwritingEngine.ts:438` — the file that runs every real VA decision — uses an uncited 18% tax-withholding estimate and an uncited 5% "Active-Duty Commissary Facility Discount" (lines 462-470). The tested, cited implementation (`underwritingNuance.ts`, `RESIDUAL_TAX_RATE = 0.22`, no discount) is **not** the code path that runs. Two questions: (a) is 18%/0.95 real, sourceable VA policy the citation docs are simply missing? or (b) is `underwritingEngine.ts` carrying stale/invented logic that should be replaced with the cited 22%-and-no-discount version? Either way, the fix routes through the guardian/scenario lane, not a direct patch. Est. 3h once you confirm the source.
2. **⛔ Find out why 6 of today's 13 scheduled executive routines never ran** (see roll-up table below): guardian (Engineering), morning-compliance, lo-pipeline, lo-edgecases, midday-lender, lo-dealteam, chaos. This is the first full day of the 13-routine suite — if it's a scheduling/wiring problem, tomorrow's suite is only as good as fixing it tonight, before it becomes an unnoticed pattern.
3. **Commit hygiene:** `kb/founder-routines/2026-07-04-bridge-bind.md` (Architecture, 7:03a) ran and produced a real WARN report but was never committed — it sat as an untracked file all day. Bundled into tonight's commit; flagging so tomorrow's routine doesn't silently lose its own report the same way.
4. Vendor-procurement's founder checklist is still open (F1 NMLS status check, F10 regulatory-subscription 30-min setup, SendGrid/Sentry env confirmation, F3/F11/F4 vendor evaluations) — unchanged from this morning's report, not re-litigated here.
5. Customer-success needs a founder/legal signoff on `kb/support-playbooks/discrimination-credit-error-escalation.md` before its scripted copy is used with a real borrower.

Runbook on file, not executed (no env vars touched, no push, no revert — nothing here rises to that level): production intake pause = set `INTAKE_PAUSED=true` in Vercel env + redeploy (`server/services/maintenanceMode.ts`, roadmap #27); bad-deploy rollback = `git revert <sha>` + push.

## Summary

The business-model check tonight is a rehearsal, not a P&L: every active rate sheet in the dev DB is still demo-version (`1.0-demo`/`v1`), and the only broker-revenue math anywhere in the codebase (lender-paid compensation basis points) is a manual, agent-referral-only calculator with no automatic booking and no cost side — so "positive margin" is true today only because nothing subtracts a cost yet, not because the business is proven profitable. The compliance posture around lead acquisition and consent is solid: TrustedForm cert is mandatory, consent IP/UA/timestamp are captured, the funnel's opt-in gating is unchanged and strict, and outbound-SMS guards (quiet hours + STOP ledger) are wired correctly — no findings there. The day's real news is structural: Risk & Model Governance found a live, uncited VA underwriting calculation that contradicts its own tested reference implementation (a genuine FAIL, now ticketed as roadmap #29), and just under half of today's newly-stood-up 13-routine executive suite silently didn't run, which is a bigger institutional risk than any single finding if it goes unnoticed for a second day. Growth, customer-success, and vendor-procurement each surfaced real but non-blocking gaps (silent lead intake, inert SLA escalation, standing founder-only vendor asks) that are already ticketed and don't change today's verdict.

## Checks run → results → evidence

### 1. Margin reconciliation — REHEARSAL, not a P&L check

Ran `rateService.syncBestExecutionRates()` live against the native dev Postgres (temporary script, executed and deleted, not committed).

- **Active rate sheets, all demo:** `demo-uwm-2026-07-02` (version `v1`, expired 2026-07-04), `SWL-2026-07-03-DEMO`, `BRC-2026-07-03-DEMO`, `AHF-2026-07-03-DEMO` (all version `1.0-demo`, effective through 2026-10-01, auto-refreshed by `seedMarketPricing.refreshDemoRateSheets()`). **No real wholesale sheet has been uploaded.** This is the exact condition the doctrine (`kb/PRE_PRODUCTION_OPS_ROUTINES.md` Routine 4) says makes the check a workflow rehearsal, not a P&L check.
- **`syncBestExecutionRates()` ran clean, synced 5 programs** at the marketing profile (780 FICO, $280k loan, $400k value / 70% LTV, 30-yr purchase, primary residence): 30yr-fixed 6.125%/6.316% APR, 15yr-fixed 5.500%/5.809% APR, FHA 30yr 6.125%/7.202% APR, VA 30yr 6.000%/6.189% APR, 5/6 ARM 5.990%/6.179% APR. These are demo-sheet outputs from the real deterministic pricing engine (`pricingAdapter.computeOffers`), not fabricated.
- **Take-rate / origination-fee reconciliation:** the codebase has no formal "take-rate" constant. Two separate fee concepts exist and should not be confused: (a) `server/services/apr.ts:125` / `loanEstimate.ts:248` compute a flat 1% origination fee as a *representative-fee-model assumption* for Reg Z APR disclosure — not a booked revenue figure; (b) `server/seedMarketPricing.ts:77,97,117` seeds a real `brokerCompensation` structure per demo wholesale lender (`defaultBps` 175–225, i.e. 1.75%–2.25% lender-paid), which is the only revenue-shaped number in the system. Applying the seeded ~200bps average to the $280k best-execution loan above: **≈$5,600 gross broker revenue on paper.** No cost (LO comp payout, processing, compliance, tech overhead) is subtracted anywhere — none of that exists in code — so "positive margin" is true today by construction, not by proof.
- **Verdict: PASS-on-paper / REHEARSAL only.** It becomes a real check the day a genuine (non-`-DEMO`/non-`v1`) wholesale sheet is uploaded via the staff rate-sheet flow, and fully real with F11 (PPE — Lender Price/Mortech).
- **Flag for the PPE migration:** the only place broker-compensation math executes at all is `calculateAgentCommission` (`server/services/optimizationEngine.ts:897-960`) — a manual, admin-triggered endpoint (`POST` via `server/routes/optimizations.ts:149`) that only fires for agent-referred loans (`user.referredByUserId` set) and looks up `defaultBps` from the demo `wholesaleLenders` table by name/ID match. Three problems that will not survive F11: (1) the majority of loans — anything not agent-referred — have **no revenue computation path in the code at all**, not even a stub; (2) it's manually triggered, not booked automatically on funding; (3) the lender-match-by-name lookup against the demo `wholesaleLenders` table has no equivalent once real compensation arrives per-quote from a PPE response instead of a static seeded table. Also confirmed dead schema: `lenderMatchResults.brokerCompensationBps/Amount` and `lenderProducts.brokerCompensationType/Bps` (`shared/schema/lending.ts:2337-2338, 2470-2471`) are declared but **never written anywhere in server code** — unused columns, not a wired feature.

### 2. First-party lead & consent audit — clean

- **(a) No purchased-lead ingestion path exists.** Grepped vendor configs and `server/mcp/vendors.ts` — no trigger-lead or list-purchase integration anywhere; only inbound `POST /api/leads` (first-party) exists.
- **(b) `POST /api/leads` still requires TrustedForm + captures consent evidence.** `server/routes/leads.ts:32` — `trustedFormCertUrl: z.string().url().max(500)` is **required** (no `.optional()`), unchanged. Consent capture confirmed at `leads.ts:94-99`: `trustedFormCertUrl`, `consentTcpaText`, `consentCapturedAt` (defaults to request time if not supplied), `consentIp` (from `req.ip`/`x-forwarded-for`), `consentUserAgent` (from request headers) are all written on every insert.
- **(c) Funnel opt-in language stays strict.** `client/src/funnel/preApprovalMachine.ts:229` gates the funnel's `final` step on `consent.softPullAcknowledged`; `FunnelContext.tsx:77-78` threads `state.consent` through every `stepGate` call — no path around it, unchanged from prior audits.
- **(d) Reg Z trigger-term disclosures on public surfaces — cross-checked against today's 1:33p Growth report rather than re-deriving.** Growth's audit of `Landing.tsx` and `AffordabilityCheck.tsx` found rate/APR shown together at equal size with the required §1026.24(c) disclaimer on the one page that shows a rate (`RatesTeaser`), and flagged one non-blocking gap (no adjacent "not a loan offer" disclaimer near `AffordabilityCheck.tsx`'s calculated payment figure — already ticketed as part of Growth's findings, not duplicated here). No violation found on either surface.
- **(e) Outbound-messaging guards intact.** `server/services/smsCompliance.ts:60-69` (`evaluateOutboundSms`) is the single required gate: opted-out check first, then `isContactAllowed` (quiet hours, `./quietHours`) — unchanged, correctly composed. No SMS sender exists yet to bypass it (confirmed by Growth's report), so there's nothing live to check for a bypass today.
- **HPPA posture:** cite the existing ledger entry (`kb/regulatory-ledger.json`, id `hppa-trigger-leads`, Pub. L. 119-36, FCRA §604(c), in force since ~2026-03-04) rather than re-verifying — our posture is structurally compliant (first-party only) regardless of the exact effective date.
- **Verdict: no findings.** Consent/lead compliance is solid.

### 3. Master triage — per-routine STATUS roll-up

| Routine | Slot | Ran? | STATUS | Headline |
|---|---|---|---|---|
| Guardian (Engineering) | 12:14a | **No report** | — | Missing |
| Security (InfoSec) | 6:33a | Yes | OK | 0 npm audit findings, baseline set |
| Bridge & Bind (Architecture, read-only) | 7:03a | Yes (uncommitted until tonight) | WARN | `/api/leads` and market-data endpoints ship with zero client binding |
| Morning Compliance | 7:51a | **No report** | — | Missing |
| LO Pipeline (LO Ops) | 8:04a | **No report** | — | Missing |
| LO Audit (Product, read-only) | 9:07a | Yes | (no formal banner; report is clean overall) | H1-H8 all verified fixed; 2 new MEDIUM findings ticketed tonight (LO-M11, LO-M16) |
| Risk & Model Governance | 10:07a | Yes | **FAIL** | VA residual-income live math contradicts its cited/tested reference (roadmap #29) |
| LO Edge-Cases (Data & Signals) | 11a | **No report** | — | Missing |
| Midday Lender (Capital Markets) | 12:31p | **No report** | — | Missing |
| Growth (GTM) | 1:33p | Yes | WARN | Zero speed-to-lead automation; 2 of 3 acquisition metric gates unmeasurable |
| LO Deal-Team (Partner Success) | 3:09p | **No report** | — | Missing |
| Chaos (Operations) | 3:35p | **No report** | — | Missing |
| Customer Success | 4:33p | Yes | WARN | SLA escalation infrastructure built but inert (empty config tables) |
| Vendor Procurement (Mon only) | 9:33a | Yes | OK | No new vendor risk; standing F1/F10/env asks unchanged |
| **Evening (this report)** | 6:30p | Yes | **FAIL** | Aggregates the FAIL above + the missing-routine pattern |

**7 of 13 daily routines ran; 6 did not produce any report today** — this is the first full day of the 13-routine suite, so there's no prior-day baseline to compare against, but a 46% no-show rate on day one warrants investigation before trusting the suite's coverage claims going forward. No honesty-rule violations found in any report that did run — every report that flagged a gap also flagged its own audit limitations (e.g., security noting `mismoExportAccess.test.ts` was read, not executed; lo-audit re-verifying rather than trusting the prior changelog).

## Corrections table

| Doctrine/report said | Verified reality |
|---|---|
| Task prompt implies a `kb/founder-routines/2026-07-04-bridge-bind.md` report should already be sitting alongside the other five | It existed on disk since this morning but was never `git add`ed/committed — an untracked file, invisible to `git log`. Included in tonight's roll-up and bundled into this commit. |
| Task doctrine (Routine 4) frames margin reconciliation as "review best-execution output against planned origination/take-rate" | There is no codified "planned take-rate" constant anywhere in the repo — the only take-rate-shaped number is the demo `brokerCompensation.defaultBps` (175-275) seeded per wholesale lender, and it's read by exactly one manual, agent-referral-only code path. Reconciled against that number since no other exists. |
| Implicit assumption that a "13-routine executive suite" running for a full day means 13 reports land | Only 7 landed today (including the two read-only ones); 6 slots produced nothing. Treated as a roll-up fact and a P1 finding, not smoothed over. |

## The prioritized backlog

**P0 — compliance/legal/security**
1. **VA residual-income tax rate + uncited discount (roadmap #29).** Owner: Amr (source-of-truth decision, required first) → Claude (implementation via guardian/scenario lane). Est: 3h once confirmed. *(Already appended to roadmap by risk-governance; not re-added.)*

**P1 — launch-blocking functionality**
2. **Investigate the 6 missing executive-routine reports** (guardian, morning-compliance, lo-pipeline, lo-edgecases, midday-lender, lo-dealteam, chaos). Owner: Amr (scheduling/infra check — this is outside my read access to the scheduler). Est: 1h to diagnose.
3. **Speed-to-lead notification on `POST /api/leads`** (roadmap G-A). Owner: Claude. Est: 3h. *(Already on roadmap from Growth; tracked, not duplicated.)*
4. **Seed `sla_class_configs`/`task_type_sla_mapping`** (roadmap CS1). Owner: Claude. Est: 2h. *(Already on roadmap from Customer Success.)*
5. **GSE submission gate ignores undeclared co-applicant** (roadmap LO-M11, new tonight). Owner: Claude. Est: 1.5h.
6. **VA zero-down advisory panel shows PMI-inclusive number next to "PMI waived" copy** (roadmap LO-M16, new tonight). Owner: Claude. Est: 1h.
7. **Bind `/api/leads`** — decide headless vs. hosted-form, then build (roadmap ARC-1, new tonight from Bridge & Bind). Owner: Amr (decision) + Claude (build). Est: 0.5h + 3h.
8. **Discrimination/credit-error escalation flag on `POST /api/messages`** (roadmap CS2). Owner: Claude. Est: 3h. *(Already on roadmap; needs the founder/legal signoff on the playbook first.)*
9. **Tag agent-sourced inbound leads** (roadmap G-B) and **cycle-time/pull-through report** (roadmap G-C). Owner: Amr (decision on G-B mechanism) + Claude (build). Est: 2h + 3h. *(Already on roadmap from Growth.)*

**P2 — polish**
10. Bind market-data moat endpoints to a staff view (roadmap ARC-2, new tonight). Owner: Claude. Est: 4h.
11. Wire `/api/scenario-calculator` into a borrower-facing what-if tool (roadmap ARC-3, new tonight — flagged by Bridge & Bind as the highest-leverage orphan). Owner: Claude. Est: 4-6h.
12. CreditConsent SSN whisper-copy (roadmap G-D). Owner: Claude (copy drafted) + Amr (glance, consent-adjacent). Est: 0.5h.
13. `INCOME_DISCREPANCY_THRESHOLD_PCT` dead/uncited constant; `NEW_TRADELINE_WINDOW_DAYS` uncited. Owner: Claude. Est: 1h each. *(Already on roadmap-adjacent from risk-governance's remediation tickets — not yet appended to CTO_ROADMAP.md itself; flagging for tomorrow's risk-governance or my next run to formalize if still open.)*

**Roadmap items appended/updated this run:** two new subsections in `CTO_ROADMAP.md` "Do next" — "LO workflow correctness (from kb/lo-audit/2026-07-04.md)" (LO-M11, LO-M16) and "Architecture / route-binding backlog (from kb/founder-routines/2026-07-04-bridge-bind.md)" (ARC-1, ARC-2, ARC-3). Grepped first for `M11`/`M16`/`market-data`/`scenario-calculator`/`orphan` — no existing entries, nothing duplicated. Roadmap #29 (risk-governance's FAIL) was already appended by that routine's own commit; not touched. The "Future" (F-) section was not touched.

---
STATUS: FAIL
