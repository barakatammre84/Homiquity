# Keep installs quiet and deterministic in CI/serverless builds (Vercel).
# audit/fund make extra network calls during install that can stall or fail the
# build; they add no value to an automated deploy.
audit=false
fund=false
