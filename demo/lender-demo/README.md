# Lender Demo Kit

Assets for pitching **wholesale / TPO lenders** to credential Homiquity as a broker.
Both tell the same **full end-to-end story**: *borrower → organized, standards-valid
MISMO package → delivered to a wholesale lender.*

| File | What it is | Use it for |
|---|---|---|
| `Homiquity_Lender_Demo.pptx` | 17-slide editable deck (Royal Blue Emerald brand), with **real product screenshots** | Live pitch, screen-share, or leave-behind. Edit freely in PowerPoint / Keynote / Google Slides. |
| `Homiquity_Lender_Demo.pdf` | Flattened PDF of the same deck | Emailing / quick preview without PowerPoint. Re-export after any edit. |
| `video-walkthrough-script.md` | Shot-by-shot screen-capture script + storyboard (~5:45) | Record a Loom/OBS demo yourself — real routes, roles, narration, and capture notes are all specified. |
| `screenshots/` | Real captures of the running MVP (landing, pre-approval, LO Command Center, borrower file, etc.) | Already embedded in the deck; reuse in the video or other assets. |

## Deck outline
Title · Problem · What we are (the core loop) · **The pipeline** · Acquire · Intake ·
Decide · **Package** · Dual AUS · Deliver · **See it live: borrower experience** ·
**See it live: broker's desk** · Why lenders win · Compliance & trust · Status (built vs. live)
· **The ask** · Appendix.

## Honesty rails (baked into both — keep them if you edit)
- **Licensing is in progress** — the pitch is to be *credential-ready*. No NMLS number is
  shown anywhere; it prints only once issued.
- **Vendor legs (credit, DU/LPA, AVM, lender submission) are deterministic simulations
  behind production adapters** — presented as *built and validated, live on credentialing*,
  never as live vendor calls.
- The engine **validates**, it does not **approve**; **AI never decides.**
- Fannie Mae / NMLS terminology matches the official specs — nothing invented.

## Before you send / present — fill these in (flagged `TODO` in the files)
- **Contact block** on the deck's "ask" slide: founder name, phone. (Email/site are set:
  `support@homiquity.com` · `homiquity.com`.) Once provided, rebuild + re-export the PDF.
- **NMLS #** — leave blank until issued (the deck says so explicitly).

## Regenerating the deck
The deck is generated by `build-deck.js` (pptxgenjs) — palette, layout, and copy are all in
that one file; it embeds the PNGs in `screenshots/`. Edit and rebuild, or ask Claude to:
```
npm install pptxgenjs react-icons react react-dom sharp   # one-time
node build-deck.js                                        # writes Homiquity_Lender_Demo.pptx
```
pptxgenjs writes an uncompressed zip; re-deflate to shrink it (any zip tool). To refresh the PDF,
render the .pptx with any tool that reads PowerPoint (LibreOffice `soffice --convert-to pdf`, or
PowerPoint's File ▸ Export). The file opens fine either way.

> Note: capturing the screenshots surfaced a real product bug — the staff **borrower file**
> showed LTV/DTI ×100 too high (`BorrowerFile.tsx` treated already-percent values as fractions).
> Fixed in the working tree during this session; commit it with the product code, not this kit.
