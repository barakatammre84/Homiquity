# Complex File Engine — ledger

Cross-run memory for the [Complex File Engine](../../.claude/skills/complex-file-engine/SKILL.md)
routine (daily 10:40 local). Each run reads this **before** Phase 1 and appends to it in Phase 4.

Three sections, three different jobs:

- **§1 Findings** — the work queue. Ids are `CF-<MMDD>-<NN>` using the **run's own date**
  (CHARTER §9: date-qualified by construction, never a bare next-free integer — six sessions once
  minted six different `F-20`s).
- **§2 Verified-not-a-defect** — **append-only.** Things a run went looking for and found already
  correctly built. This section is the guard against the house's most expensive research failure:
  *"the backend is far smarter than the UI surfaces it — grep before building anything a doc calls
  missing."* A row here is never re-investigated from memory.
- **§3 Refusals** — **append-only.** Work deliberately not done, and the gate that would reopen it.
  A path refused for a missing program reference is never re-derived by a later run.

Seeded 2026-08-18 by the founder session that registered the routine. Every row below was verified
against the code in that session at `de3c44e1`, not carried over from a document.

---

## §1 Findings

| id | status | class | what | evidence | disposition |
|---|---|---|---|---|---|
| `CF-0818-01` | **fixed** | drift hazard | `INCOME_PATH_IDS` is declared **twice** — canonically in `shared/incomePaths.ts:30` and again in `shared/situationProfile.ts:31`. Both lists are identical today, and nothing guards that. A sixth path added to the canonical enum leaves the SituationProfile's `z.enum` silently rejecting it, so a newly-supported path would not appear in the profile that tells the LO which paths apply. | both files read 2026-08-18; ids identical (`agency_wage, self_employment, rental, dscr, bank_statement`); no drift test found | **SHIPPED 2026-08-19** — `situationProfile.ts` now re-exports the canonical enum by name (never `export *`); a new colocated test pins them by **identity**, not deep equality, because a copy would still satisfy `toEqual`. Mutation-proven. |
| `CF-0818-02` | open | **C7 coverage gap** | Capital-gains income is *detected* (`server/services/situationClassifier.ts:180-185`) and *reconciled* (`server/services/taxReconciliation.ts:271`) but **no income path computes it** — `INCOME_PATH_IDS` has no capital-gains member, so a borrower whose income is substantially capital gains is flagged and then contributes nothing to qualifying income. This is the exact defect class the routine exists to find: detected, surfaced, and then dropped before it can qualify anybody. | `shared/situationProfile.ts` `SITUATION_FLAG_IDS` contains `capital_gains_present`; grep for `capitalGain` across `server/services` + `shared/incomePaths.ts` returns only the classifier and the reconciler | **PROPOSE ONLY — C2 + C3.** Capital-gains qualifying income is regulated math (Fannie Selling Guide B3-3.1-09: history and continuance requirements), and `docs/fannie-mae/` holds **no Selling Guide income authority for it** — the only income references in-repo are `self-employment-income-reference.md` and `rental-income-reference.md`. So this is a **procurement ask first** (transcribe B3-3.1-09 into a reference doc), then a ledger citation, then a path. No code until the doc lands. |
| `CF-0818-03` | standing ⛔ | authority gap | The `dscr` and `bank_statement` paths are hard-blocked `PROGRAM_REFERENCE_NOT_IN_REPO` — correctly. The blocking facts are the Angel Oak **DSCR minimums by LTV/FICO** and the **deposit-eligibility rules**, both portal-gated. | `server/services/income/paths/dscr.ts:128-132`, `paths/bankStatement.ts:142-147`; `docs/lender-programs/angel-oak/` holds the two transcribed references but not these matrices | **Founder action, restated every run.** Obtain from the Angel Oak AE. Until then the paths compute no figure — **never soften this (C4)**. |

| `CF-0819-01` | **fixed** | **C7 root blocker** | **Section 1e income type was a display string, not a value.** The 20 types a borrower can declare lived only in a client-only constant (`client/src/pages/borrower/urla/types.ts:174`) as human-readable labels — `"Retirement (e.g., Pension, IRA)"` — written into a free-text `other_income_sources.income_source varchar(100)`. Nothing server-side could classify them, so `agencyWage.ts` summed **every** source at 100% of face value. This single fact blocked four capabilities at once: gross-up, continuance, a capital-gains path, and per-type document requests. | `agencyWage.ts` other-income loop, pre-fix: `variable += toNum(o.monthlyAmount)` with no type read; `INCOME_SOURCES` had exactly one consumer and no server twin | **SHIPPED 2026-08-19** — a new shared Section 1e catalog module (`incomeTypes`, lands with the follow-up PR) is the one vocabulary; the picker renders it (byte-identical strings, so **no stored row changes meaning and no migration**); `classifyOtherIncomeSource` reads the column and returns `null` rather than guessing. Not regulated math — no figure moved. |
| `CF-0819-02` | open | ⛔ authority gap | **No non-taxable gross-up exists anywhere in the codebase** (`grossUp`/`nonTaxable`/`1.25` → no hits in `server/services` or `shared/`). Social Security, Disability, VA Compensation, Public Assistance and Foster Care are all counted at declared face value. If agency policy permits grossing these up, the platform is **systematically under-qualifying** exactly the fixed-income borrowers a broker should win. | grep over `server/services` + `shared/`; `agencyWage.ts` applies no factor to any Section 1e source | **PROPOSE ONLY — C2 + C3.** ⚠️ The gross-up rule is **flagged, not asserted**: `docs/fannie-mae/` holds income references for self-employment and rental **only** — there is no Selling Guide income chapter in-repo, so no reading can be verified. **Founder action: obtain B3-3.1-09.** Then a ledger citation, then `qualifyingAuthority` on the affected catalog entries — its colocated test fails any citation naming a file not on disk. |
| `CF-0819-03` | open | ⛔ authority gap | **No continuance test exists** (`continuance`/`remainingMonths` → nothing in the income path). Alimony, Child Support, Separate Maintenance and Unemployment Benefits count at 100%, indefinitely. Where continuance is required before income counts, this can **over-qualify** — the opposite error to `CF-0819-02`, from the same missing classification. | same grep; `agencyWage.ts` has no duration or end-date input for Section 1e sources | **PROPOSE ONLY — C2 + C3.** Same authority as `CF-0819-02` and same gate. Note the capture gap too: `other_income_sources` stores only `income_source` + `monthly_amount`, so even with the rule in hand there is **no field to record continuance in** — that part is a schema change and belongs to the Backend Data Engineer. |
| `CF-0819-04` | open | **C7 coverage gap** | **No asset-depletion / employment-related-assets path.** The only `depletion` hits in the repo are the Form 1084 *tax add-back* (`selfEmploymentIncome.ts:63,209`), which is an unrelated concept. A retired or asset-rich borrower with little declared income qualifies for nothing. | grep `assetDepletion\|employmentRelatedAsset` across `server`, `shared`, `client` → no hits | **PROPOSE ONLY.** Regulated math with no in-repo authority; same procurement gate as `CF-0819-02`. Ranks high on the move-up/affluent lane. |
| `CF-0819-05` | open (downgraded) | question A (delivery) | **The MISMO package carries no income itemisation.** `CurrentIncomeItem` — with a **required** `incomeType` — is modelled in `shared/mismo.ts:842` and **never populated by `server/mismo.ts`** (zero references to `currentIncome` or `CurrentIncomeItem` in the emitter). | `grep -n "currentIncome\|CurrentIncomeItem" server/mismo.ts` → no output | **Downgraded on evidence, same day — not a schema-validity defect.** Verified against the MISMO 3.0 XSD shipped in-repo (`docs/fannie-mae/schemas/uldd-phase5-extension/MISMO_3_0.xsd`): **every `CURRENT_INCOME*` element is `minOccurs="0"`** — `CURRENT_INCOME`, `CURRENT_INCOME_DETAIL`, `CURRENT_INCOME_ITEMS`, `CURRENT_INCOME_ITEM_DETAIL`, and `CURRENT_INCOME_ITEM` (`maxOccurs="unbounded" minOccurs="0"`). The package therefore validates without it, and **no delivery edit in `shared/fannieMae/loanDeliveryEdits.ts` references income at all**. (`mismoValidation.ts:209` does score other income, but that is URLA section completeness, a different gate.) ⚠️ **What is still unverified is ULDD *conditionality*** — a data point can be conditionally required by the spec while optional in the XSD, and the Loan Delivery job aid returns 403 per `CLAUDE.md`. So: not a P1, not closed. Whoever takes it reads the ULDD Phase 5 spec PDFs already in `docs/fannie-mae/` rather than answering from memory. `CF-0819-01` is its prerequisite either way — a MISMO `incomeType` enum cannot be populated from a display string. |
| `CF-0819-06` | **partly fixed** | drift hazard | **Three income vocabularies that cannot drift-check each other:** the funnel enum (7 values, `shared/preApprovalForm.ts:88`), the URLA catalog (20 labels), and `SEASONING_GOVERNED_TYPES` (4 values, `underwritingNuance.ts`). A borrower's income type is re-expressed at every boundary it crosses. | the three declarations, read 2026-08-19 | **The URLA leg shipped** with `CF-0819-01`. The funnel↔URLA and seasoning legs are open — reconciling them touches the funnel schema, so scope it with the Backend Data Engineer rather than unilaterally. |

## §2 Verified-not-a-defect (append-only)

| verified | claim that was tested | outcome |
|---|---|---|
| 2026-08-18 | "Declining self-employment income has no treatment — averaging would overstate it." | **FALSE — already built and cited.** `server/services/selfEmploymentIncome.ts:100-121` implements B3-3.5-01: on a year-over-year decline it uses the most recent (lower) year, never averages the decline up, and flags manual analysis. `trend` is a first-class field of the result. Do not re-report. |
| 2026-08-18 | "UAL P4's `fs` citation guard — asserting every enabled path's authority doc exists on disk — was never built." | **FALSE.** `tests/nonQmProgramGate.test.ts` exists and does exactly that. It is the mechanical form of "no citation, no implementation"; keep it green, do not rebuild it. |
| 2026-08-18 | "The P5 import boundary — no underwriting-side module may import `taxInsightService` — is documented but unenforced." | **FALSE.** `tests/accuracyLoop.test.ts:220` enforces it by reading sources. `taxInsightService` is imported only by `server/routes/documents.ts` and `server/routes/taxInsights.ts`. Keep it. |

## §3 Refusals (append-only)

| refused | what | why | reopens only if |
|---|---|---|---|
| 2026-08-18 | Computing any DSCR pass/fail verdict, or any bank-statement figure, from published/marketing sources or from the model's own knowledge. | C2/C4. A wrong non-QM number leaks into a lender package and cannot be recalled. The TPO portal matrix controls, and it is not in-repo. | The AE matrix is transcribed into `docs/lender-programs/angel-oak/` (founder action `CF-0818-03`). |
| 2026-08-18 | Editing `server/underwritingEngine.ts`, `server/services/decisionEngine.ts`, `server/services/ruleEngine.ts`. | C1 / CHARTER §10 + §6: credit policy is L4, human-only — it is what the licensee is accountable for. | Never, for this routine. Engine changes are proposals in the report; the founder executes them. |
| 2026-08-18 | Writing to `data/regulatory/regulatory-ledger.json` to accompany a regulated-math change. | C3. CHARTER §10 puts `data/regulatory/**` off limits to every routine *and* requires a same-commit ledger citation for regulated math — together they mean this routine may not change regulated math at all. It drafts the entry verbatim in the report instead. | The founder amends CHARTER §10 knowingly (§6: rails are never relaxed by a routine or by a session acting for one). The proposal is in the routine's registration report, 2026-08-18. |
