# Security Posture — 2026-07-04

> **⛔ ARCHIVED 2026-07-08 — launch-era snapshot (2026-07-02 → 07-06), superseded. Do not act on this document.** Current truth lives in the 🚀 Launch sprint of [CTO_ROADMAP.md](../../../CTO_ROADMAP.md); see the [archive rationale](../README.md). Retained for history only; its dated findings are preserved as written.

**STATUS: OK** — no committed secrets, no PII in logs, no broken access-control guard on new surface, 0 npm audit findings, 478/478 unit tests + tsc clean. First run of this routine — no prior report exists to diff against, so this establishes the baseline inventory.

## Human actions

None required today. Standing asks already tracked elsewhere (Sentry/SendGrid env vars, F3 vendor SOC 2/DPA ask) are owned by the vendor-procurement routine (`kb/founder-routines/2026-07-04-vendor-procurement.md`) — not duplicated here.

## Summary

Ran a dependency audit, a secrets-hygiene sweep, a PII-in-logs grep, and an access-control check on every route touched in the last 48 hours (which included today's LO Command Center PR #33 and yesterday's H8 MISMO-export fix). Nothing failed. `npm audit` came back clean at 0/0/0/0. No secret patterns in the last 48h of commits — the one regex hit was a hardcoded placeholder test DB URL, not a real credential. The MISMO export route (previously the H8 SSN/DOB leak) is still internal-staff-only on both server and client, and the new LO Command Center's client gate correctly uses `isInternalStaffRole`, not the wider `isStaff`. Today's deep-dive rotation (upload path handling) found the existing filename-generation scheme is already traversal-safe.

## Checks run → results → evidence

**1. Dependency audit** — `npm audit --json`: `{info:0, low:0, moderate:0, high:0, critical:0}` across 775 total deps (605 prod). No baseline to diff against (first run); this number is now the baseline for tomorrow's delta.

**2. Secrets hygiene** —
(a) `git ls-files | grep -iE '\.env|credentials|secret'` → only `.env.example` and `kb/app-guide/06-auth-security-secrets.md` (a doc, not a secret file). `.gitignore` covers `.env` / `.env.local`. Clean.
(b) `git log --since="48 hours ago" -p` scanned for key/password/private-key/postgres-URL patterns → one hit, [tests/...](tests) fixture at the literal string `"postgresql://unit-tests:placeholder@localhost:5432/unit_tests_never_connects"` — a deliberate placeholder, not a live credential. No real secret found.
(c) No secret values printed in this report per policy.

**3. PII-in-logs scan** — grepped `server/` for `console.*` calls interpolating ssn/dob/dateOfBirth/creditScore/account-number. One match: [server/routes/borrower.ts:495](server/routes/borrower.ts:495) `console.error("SSN reveal error:", error)` — logs the caught exception object, not the `ssn` variable itself (confirmed by reading lines 467–498; `ssn` is only ever passed to `res.json`, never to console). Not a leak.
`ssnVault` remains the only encrypt/decrypt path (`server/services/ssnVault.ts`, unchanged in the last 48h per `git log --stat`); no new plaintext SSN column writes found in `shared/schema.ts` (only unrelated 1-line change in the 48h window).

**4. Access-control regression on new surface** — `git log --since="48 hours ago" --stat -- server/routes/` reviewed commit-by-commit:
- [server/routes/lending.ts:863](server/routes/lending.ts:863) MISMO export — still `requireRole("admin","lo","loa","processor","underwriter","closer")`, matching the H8 fix from PR #26/commit `9e94dee`.
- LO Command Center (PR #33, `0522b04`) added no new Express routes — it reuses the already deal-team-scoped `GET /api/pipeline/queue` ([server/routes/underwriting.ts:657](server/routes/underwriting.ts:657)) and the H8-gated MISMO export. Client gate at [client/src/pages/staff/LoCommandCenter.tsx:119](client/src/pages/staff/LoCommandCenter.tsx:119) uses `isInternalStaffRole(user?.role)` — correctly the internal-staff check, not the broader `isStaff`.
- Kill-switch/AAN commit (`5d2af55`) touched `compliance.ts`/`leads.ts`/`lending.ts` — additive gating only (maintenance-mode 503 short-circuit), no guard removed.
- `tests/mismoExportAccess.test.ts` is an **integration** test (needs a live server+DB, not in the unit vitest include list) — verified the guard by reading the route code above rather than executing the suite, since starting a server was out of scope for a read-only pass. Flagging this as a gap: the tripwire wasn't executed, only statically confirmed.

**5. Auth surface spot-check** — auth tokens: `createHash("sha256")` confirmed in `server/services/accountRecovery.ts:14`; login/authLimiter and leadsLimiter present in `server/app.ts` (lines 86, 142, wired at 176); helmet config at `server/app.ts:65` unchanged — CSP directives + report-only mode intact, no weakening found. **Today's rotation focus: upload path handling.** `server/routes/utils.ts` generates filenames as `Date.now() + random + path.extname(originalname)`; verified via a quick Node repl that `path.extname` returns `""` for any crafted `originalname` containing a `/` after the last `.` (e.g. `"a.pdf/../../../etc/passwd"` → `""`), so directory-traversal characters can't survive into the on-disk filename. No finding.

**Standing gaps (say so every time):** there is no real SAST or DAST here — "SAST" is `npm audit` + `tsc` + targeted greps; there is no dynamic scanning at all. No signed build artifacts. No CI gate on `main` (roadmap #5 — token lacks `workflow` scope, manual add still pending). SMS webhook signature verification remains a documented stub (`server/routes/webhooks.ts:17`) until a real provider is contracted. No CORS middleware exists — same-origin architecture, unchanged, not flagged as new risk.

## Corrections table

| Doctrine/prompt said | Verified reality |
|---|---|
| Task prompt implies a standing `kb/founder-routines/` security report to diff against | No prior security report exists — this is the inaugural run; today's numbers become the baseline. |
| Task prompt's tripwire #4 implies `tests/mismoExportAccess.test.ts` should be run and confirmed green | It's an integration test requiring a live server/DB; not executed this run (static code read only) — see gap above. |

## Remediation tickets

None. No new P0/P1 found — nothing met the FAIL or ticket-worthy bar. Existing roadmap items (#1 uploads-to-prod, #5 CI-on-main, F3 vendor contract) already cover the only open threads touched by this audit; not duplicating them.

---
STATUS: OK
