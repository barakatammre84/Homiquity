# Growth GTM Routine — 2026-07-04 (midday re-check)

> **⚠️ SUPERSEDED SAME EVENING (banner added 2026-07-04; snapshot preserved as written):**
> the persona-LP branch landed as PR #42 (with the G-E route-collision fix — education hub
> moved to `/learn/first-time-buyer`) and speed-to-lead shipped as PR #40, both in the 13-PR
> launch batch merged to main this evening. The still-open items from this report are G-B
> (agent-source tagging) and G-C (cycle-time report) — both deliberately deferred.

## STATUS: WARN

marketing_signoff: WARN — no Reg Z/Reg N violation on any live public surface and no consent guard bypassed, but speed-to-lead has zero automation (every new lead is a silent DB row), two of the three acquisition metric gates (cycle-time, agent-sourced %) still can't be measured even once real traffic arrives, and the still-unlanded persona-LP branch now has a route collision that would silently break a live page if merged as-is.

*Note: this is a same-day re-run of the routine (earlier report committed at `3e12cb4`). All findings from that run were re-verified against current code and are unchanged except one new finding (route collision, item 5 below).*

## Human actions (Amr)

**Campaign consent checklist** — read this before approving ANY campaign:
- [ ] First-party opt-in only + TrustedForm cert — **enforced in code** (`trustedFormCertUrl` is a required field, `server/routes/leads.ts:32`, not optional). We never buy trigger leads (doctrine, unchanged).
- [ ] Quiet-hours + STOP ledger on any SMS — **built** (`server/services/quietHours.ts`, `sms_opt_outs` table), but no outbound SMS sender exists yet to point at it, so there's nothing to launch today.
- [ ] RESPA §8 — no paid referral arrangements dressed as marketing. No new arrangements observed this cycle; nothing to review.
- [ ] Co-branded LO landing pages carry correct licensing disclosures — **N/A today, none exist yet** (see LO-partner GTM backlog).
- [ ] Every numeric claim carries its Reg Z disclosures — **verified below**, no violations found on current public surfaces.

⛔ No FAIL-tier items this cycle. Nothing needs an intake pause or rollback.

Human-only items (not automatable by Claude):
- Decide whether to greenlight a real speed-to-lead notification (staff Slack/email ping + borrower auto-ack) — this is the single highest-leverage conversion fix available and it's pure engineering, but wiring it to a real inbox/Slack needs your provider choice. (Unchanged from last run — still not started.)
- Decide the source-tagging convention for agent-referred inbound leads before the first agent B2B2C leads start landing, or they'll be unattributable retroactively. (Unchanged.)
- Before merging the persona-LP worktree branch (`claude/distracted-ramanujan-35682b`), resolve the new `/first-time-buyer` route collision (item 5 below) — otherwise the existing education page silently goes dark.

## Summary

This re-check confirms the prior WARN findings are all still accurate and none have regressed or shipped: `POST /api/leads` still creates a lead and does nothing else (G-A), agent-sourced attribution still doesn't exist (G-B), the cycle-time/pull-through report still isn't built on top of otherwise-available audit data (G-C), and the CreditConsent SSN field still lacks whisper-copy (G-D). Friction data is unchanged — still 100% compliance-gate friction (eDisclosure, anti-steering), still handled well by the client, still no fix needed. The three persona landing pages (refinance, VA, self-employed, first-time-buyer) built earlier today remain on the unlanded worktree branch and are correctly not treated as live surfaces. New this run: tracing that branch's `App.tsx` against `main` found that its `/first-time-buyer` route will collide with an existing, live education page (`FirstTimeBuyerHub`) at the same path — a merge-time landmine worth fixing before that branch ships, filed as G-E.

## Checks run → results → evidence

### 1. Funnel conversion friction (7-day window, `GET /api/jobs/friction-summary?days=7`, admin@test.com)

**Data is 100% seed/test traffic in the dev DB — no real borrowers yet. Labeled accordingly, not treated as a conversion rate.**

```
byPoint: consent_gate_blocked = 10, anti_steering_blocked = 1
```

Identical distribution to the prior run today. All 10 `consent_gate_blocked` events carry `detail: "e_disclosure"` on `POST .../loan-estimate`. Re-confirmed the client resolves this well: `LoanEstimate.tsx` renders `ConsentGateCard` (`client/src/components/ConsentGateCard.tsx`) on the 403 with the disclosure text + acknowledgment + auto-retry, rather than a dead end. **Verdict: qualification/compliance friction, working as intended — no fix needed.** The single `anti_steering_blocked` event is the same L3 gate on LoanOptions.

**CreditConsent SSN whisper-copy gap — re-verified still open**, byte-identical to the last check: `client/src/pages/borrower/CreditConsent.tsx:358-368`, label + placeholder only, no trust microcopy. Proposed copy unchanged:

> "We only use this to match you with your credit report. It's encrypted the moment you submit it and never shown to loan officers."

Not shipped this cycle either — copy change on consent-adjacent UI needs a founder glance first per standing guidance.

### 2. Landing-page & copy audit

Re-confirmed public surfaces on `main`: `/` (`Landing.tsx`), `/afford` (`AffordabilityCheck.tsx`), `/rates`, `/rates/refinance`. Re-confirmed `App.tsx` has no `/refinance`, `/va-loans`, `/self-employed`, or persona-specific `/first-time-buyer` route — the whole 4-page persona-LP set (commits `1c901ae`, `1e17e77`, `2617d15`, `d7ef918`) remains on worktree branch `claude/distracted-ramanujan-35682b` only. None audited as live surfaces. No new Reg Z/Reg N issues found on `Landing.tsx` or `AffordabilityCheck.tsx` beyond what was already flagged (the missing inline disclaimer near `AffordabilityCheck`'s calculated payment figure, still open, not a violation — no numeric product-rate claim, just a UX nicety).

### 3. Speed-to-lead path (`POST /api/leads`, `server/routes/leads.ts:64-135`)

Re-traced the handler line by line: validate → idempotency check → `storage.createLead` → `logAudit("lead.created", ...)` → return `{id, status}`. Still no notification, no assignment, no ack email anywhere in the path (confirmed by grep for `sendNotificationEmail`/`notifyStaff`/`createNotification` in this file — zero hits). `leadsLimiter` and `intakePausedGate` both still correctly wired. Unchanged from last check — G-A remains the top lever.

### 4. Metric-gate readiness (70% pull-through / 12-day cycle / 30% agent-sourced)

Re-verified, unchanged:
- **Pull-through & cycle-time:** raw plumbing exists (`audit_logs` `loan_application.status_changed` with `previousStatus`/`newStatus`/timestamp, `server/routes/lending.ts:1496`), report query still not built (G-C).
- **Lead → application attribution:** exists (`leads.convertedApplicationId`/`convertedAt`, `shared/schema/leads.ts:70-72`).
- **Agent-sourced %:** still structurally unmeasurable — `leads.source` enum has no agent-referral value; `agent_referral_requests` is still the reverse (outbound) direction only (G-B).

### 5. NEW — route collision on the unlanded persona-LP branch

Diffing the worktree branch's `App.tsx` against `main`'s: `main` already serves `/first-time-buyer` → `FirstTimeBuyerHub` (an existing education page, `client/src/pages/education/FirstTimeBuyerHub.tsx`, live today). The worktree branch adds a second, earlier `<Route path="/first-time-buyer" component={FirstTimeBuyer} />` (the new persona LP) into the same `<Switch>`, ahead of the pre-existing `FirstTimeBuyerHub` route. `wouter`'s `Switch` renders only the first matching `Route`, so merging that branch as-is would silently make the education hub unreachable at `/first-time-buyer` — no type error, no build failure, just a page going dark. The other three persona LPs (`/refinance`, `/va-loans`, `/self-employed`) don't collide with anything on `main` today. Filed as G-E — needs a path rename on one side before that branch merges.

## Corrections table

| Doctrine/memory said | Verified reality | Action taken |
|---|---|---|
| Persona LPs (refinance/VA/self-employed/first-time-buyer) "built, unlanded" | Re-confirmed unlanded on `main`; not audited as live. | No change — still correctly excluded from the live-surface audit. |
| No known issue with the persona-LP branch beyond "unlanded" | Branch introduces a route collision with an existing live education page at `/first-time-buyer` | New ticket G-E filed in `CTO_ROADMAP.md`. |
| G-A/G-B/G-C/G-D open | All four re-verified still open, no drift | No new tickets needed for these; existing roadmap entries stand. |

## Remediation tickets

G-A through G-D already exist in `CTO_ROADMAP.md` "Do next" → Growth/acquisition wiring, unchanged this cycle (re-verified, not duplicated). One new ticket added in this cycle's commit:

1. **G-E. `/first-time-buyer` route collision** — Owner: Claude. Est: 0.5h. Rename one of the two colliding routes (education hub → e.g. `/learn/first-time-buyer`, or the persona LP → e.g. `/lp/first-time-buyer`) before the persona-LP branch merges; add a route-collision check to that PR's description.

STATUS: WARN
