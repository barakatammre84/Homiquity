# Deliverable QA Sweep — 2026-08-20

STATUS: FAIL — two new P1s, and the CI gate has been dead for 29 hours while 27 commits merged to `main` ungated.

COVERAGE: domains **10/14** reviewed · workflows **8/8** run · open P0 **0** · open P1 **28** · oldest unfixed **F-020 (43 days)**

> **Workflow coverage is complete for the first time in this program's history.** The analytics
> feedback loop was the last never-run entry, had no scripted outline at all, and was the `+` row in
> the ledger for seven weeks. It now has a 10-step executable script (`WORKFLOWS.md` §8) and a run
> behind it. Domains 11, 12 and 13 remain unreviewed.

> **And the tooling gap that crippled the last six passes is closed.** Domains 5, 6, 7, 8 and
> Workflows 3, 6 all reported *"no dev server — the `:5002` listener is an N-day-old orphan from a
> deleted worktree."* This run started a real one from the review worktree and **proved its identity
> before using it** (PID 83460, `lsof -a -p 83460 -d cwd`, `ps` START, and `/api/health` carrying the
> `commit`+`email` keys current code returns). Every finding below is backed by executed HTTP, SQL, or
> a rendered component — not by a code trace. That is the difference between this report and the last six.

**Targets and why.** Domain **10 (Borrower graph & data intelligence)** — the lowest-numbered domain
never reviewed; no tiebreak needed. Workflow **the analytics feedback loop** — the only entry in the
ledger never run, and it happens to be Domain 10's own closed loop, so the two targets test the same
subsystem from opposite ends. That pairing is why three agents independently hit the same two crashes.

---

## ⛔ Human actions — hardest first

### 1. GitHub Actions billing has failed. The CI gate has not run for 29 hours, and 27 commits merged to `main` behind it.

This is not inference. GitHub's own annotation, retrieved verbatim:

```
$ gh api repos/:owner/:repo/check-runs/96531339030/annotations
"The job was not started because recent account payments have failed or your
 spending limit needs to be increased. Please check the 'Billing & plans' sec…"
```

The signature is unmistakable in the job record — `runner_id: 0`, `runner_name: ""`, `steps: []`,
and a 2-second failure. **Every PR gate run today failed this way**, 40 of the last 40:

```
$ gh run list --event pull_request --limit 200 --json conclusion --jq 'group_by(.conclusion)|map({c:.[0].conclusion,n:length})'
[{"c":"cancelled","n":19},{"c":"failure","n":78},{"c":"success","n":103}]

last GREEN gate run: 2026-08-19T14:06:26Z          ← 29 hours ago
commits merged to main since:  27
```

**And nothing stopped them, because the required check does not exist.** `ci.yml` carried a comment
claiming *"✅ CONFIGURED 2026-07-17: gate required · enforce_admins ON"*. The live API says otherwise:

```
$ gh api repos/barakatammre84/Homiquity/branches/main/protection
"required_status_checks":{"strict":false,"contexts":[],"checks":[]}
```

*(The doc half of this was found and corrected independently while this sweep ran — `ded1b798`, PR
 #635, *"the gate comment claimed a required check that main does not have"*. Cited, not re-reported.
 **The billing outage itself is still live and still unowned, and it aged another day** — yesterday's
 sweep reported the same failure class at the same repo.)*

**The reassuring half, and I ran it so you would not have to guess.** `main` @ `fed1458e` is green
on every gate step locally:

```
pnpm check                     EXIT=0
pnpm test    node lane   207 files / 3030 passed | 1 skipped
             client lane 111 files /  728 passed          EXIT=0
pnpm guard:schema              OK — every schema column is migrated or baselined
pnpm guard:ui                  OK — 508 files, 9 metrics at or below baseline
pnpm audit --prod --audit-level=high    5 vulns: 1 low, 4 moderate → passes the gate's threshold
```

So the outage has **not yet let a broken commit through**. It has removed the control that would
tell you when it does. `pnpm guard:bundle` was the only step I could not run — it measures a build,
and `pnpm build` was out of scope for a findings-only run.

**Decision needed: raise the spending limit / fix the payment method, and decide whether the `gate`
check should be a required context on `main`.** Prod is current and healthy throughout
(`GET /api/health` `commit` == `origin/main` tip, checked three times across the run).

### 2. Two new P1s, both crashes, both in shipped surfaces, both invisible to every existing guard.

- **F-0820-20** — the staff **Intelligence tab throws and unmounts the entire routed app**, and has
  since the initial commit on 2026-06-01. `GET /api/outcomes/funnel` has always returned a flat
  object; the client has always typed it as an array and called `.map()` on it. The only error
  boundary wraps `<Router/>`, so a staffer loses navigation, pipeline and queue — not just the tab.
- **F-0820-21** — **recording a loan outcome breaks the prediction that outcome exists to inform.**
  `numeric` returns from node-postgres as a *string*, so `convRate?.toFixed(4)` throws.
  `/api/predictions/*` answers **200 while a credit bucket is empty and 500 the moment it holds one
  resolved outcome**, and there is no `GROUP BY` — six resolved outcomes break 100% of borrowers.
  The borrower's insights card then vanishes with no error at all (`if (!prediction) return null`).

Both belong to the **Primary Engineer's** next run. Neither needs a decision from you.

### 3. Decide whether `MODEL_RISK_GOVERNANCE.md` is ratified or draft.

It has read `DRAFT` since 2026-07-03 while the root README cites it as authority. Two findings this
run (F-0820-38, and part of F-0820-27) are gaps *against an unratified document*, which makes their
severity a judgement call rather than a finding. One line from you settles both.

### 4. One question no code can answer: does the borrower's credit-consent text cover internal portfolio analytics?

`aggregateAnonymizedData` iterates **every** borrower and reads each one's bureau-derived credit
score to build cohort aggregates (`server/routes/jobs.ts:212`). Suppression is real and verified
(`if (cohort.count < 5) continue`), so the *output* is genuinely aggregate — but the *input* pass is
a secondary, non-transactional use of bureau data across the whole book. That is a consent-text and
counsel question, not a code question. Recorded as an escalation, not as a finding.

---

## Summary

Domain 10 and the analytics loop are the same machine seen from two ends, and both ends are broken in
the same place: **the write half works and every read-back is wrong.** Outcomes are captured correctly
and the aggregation SQL is value-exact against the database field-for-field — then the staff surface
fails to mount, the borrower prediction 500s as soon as the loop has data, and the cohort table that
feeds the benchmark is empty by construction from three independent causes, one of which is a weekly
cron selecting a user role that exists in no role list and on none of 482 users. Twenty-seven findings
were registered, five refuted and three merged rather than re-minted. The single most valuable result
is not a defect: **the Reg B firewall is real**, verified on two independent axes by two agents that
never saw each other's work, with a 45-term prohibited-basis census returning zero and the commands
recorded so the next session can re-run them. What the firewall lacks is a guard — and while attacking
that claim the verifier found something worse and separate, that **the CI invariant protecting the
adverse-action path reads a 14-line re-export shim instead of the 662-line module that holds the code**,
proven by a control run that stayed green with the LLM narrator imported directly into the adverse-action
generator.

---

## Evidence

### The live server — proved, then used

```
$ curl -s localhost:5002/api/health
{"status":"ok","timestamp":"2026-08-20T18:12:46.405Z","commit":null,"email":{"configured":false,"providers":[]}}
$ lsof -a -p 83460 -d cwd
node  83460  …  cwd  DIR  …/scratchpad/qa-wt          ← the review worktree, @ fed1458e
$ ps -o lstart= -p 83460
Thu Aug 20 15:11:39 2026
```

`commit: null` **is** the local-dev signature. The presence of the `commit` and `email` keys is what
proves it is current code rather than the 13-day orphan that poisoned four prior passes. ~154
authenticated requests were served across the run.

### The two P1s

```
$ curl -s -b admin.jar -H 'X-Forwarded-Proto: https' localhost:5002/api/outcomes/funnel
{"submitted":126,"preApproved":107,"conditional":0,"clearToClose":0,"funded":0,"denied":2,
 "withdrawn":1,"conversionRate":0,"avgDaysToClose":null,"avgDaysToPreApproval":"0.0"}   ← an OBJECT
```
`client/src/pages/staff/IntelligenceTab.tsx:19-23,53-55` types it `FunnelData[]`; `:96`
`funnelStages = funnel || []`; `:175` `funnelStages.length === 0` is `undefined === 0` → false;
`:181` `.map()`. Rendered against that verbatim body through the **real** `AppErrorBoundary`:

```
TypeError: funnelStages.map is not a function
Something went wrong   The page failed to load. This is usually fixed by reloading.   Reload
document.body length after render = 0        ← the whole tree unmounted
```
`git log -S "FunnelData" --follow` → one commit, `f89838cb`, 2026-06-01. **F-0820-20.**

```
buyer@test.com   (score 600 → bucket below_640, 2 resolved outcomes)  /api/predictions/me → 500
renter@test.com  (score 710 → bucket 680-719,   0 resolved outcomes)  /api/predictions/me → 200
Prediction error: TypeError: cohortData?.convRate?.toFixed is not a function
    at computePrediction (server/services/predictiveEngine.ts:235:49)
$ node -e "…select (1::numeric/3::numeric)::numeric(5,4)"   →  '0.3333'   typeof 'string'
```
A matched A/B on **credit bucket, not on time** — the only difference between 200 and 500 is one row
in `loan_outcomes`. **F-0820-21.**

### The finding that only appeared under attack

`tests/complianceInvariants.test.ts:367` guards the adverse-action path by reading
`server/services/creditService.ts` — a **14-line `export * from …` shim** (split 2026-07-17). The
§1002.9 notice text, the reason mapping and the `ensureAdverseActionForDenial` chokepoint all live in
`server/services/creditAdverseActions.ts` (**662 lines**), which the guard never opens. Control pair,
on throwaway edits, both reverted with `git status --porcelain` empty afterwards:

```
+ import Anthropic from "@anthropic-ai/sdk";      → decisionEngine.ts   RED   1 failed / 78 passed
+ import { buildRiskBrief } from "./riskBrief";   → creditAdverseActions.ts   GREEN  79 passed
```

The second is the LLM risk brief imported directly into the module that generates adverse-action
notices, with the suite green. `MODEL_RISK_GOVERNANCE.md:19` sells exactly this control to examiners.
Re-verified against `origin/main` @ `ded1b798` **after** `complianceInvariants.test.ts` changed the
same afternoon — the 08-20 diff touched only the VA-constants block; `:367` still names the shim.
**F-0820-22.**

### The CLEAN result that matters most

The **Reg B firewall holds**, checked on two axes by two agents independently. No output of
`predictiveEngine` / `optimizationEngine` / `borrowerGraph` / `signalEngine` / `intentTracker` /
`outcomeTracker` reaches a credit-decision path — verified by module imports **and** by direct
table/field references across all eleven decision-path files, every count zero. `loanAnalysis.ts:437,448,468`
are the only edges and they run **outbound**. A 45-term prohibited-basis census across seven service
files returned zero for race, ethnicity, color, religion, national origin, sex, gender, public
assistance, disability, **zip code**, surname, language, neighborhood and age. `citizenship` *is* a
decision input at `lenderMatchingEngine.ts:175-191` and is **expressly permitted** by 12 CFR
§1002.6(b)(7), retrieved verbatim and body-grepped. And *"the model said so"* is structurally
impossible in a notice: `creditAdverseActions.ts:28-30` accepts only enumerated reason keys — free
text cannot enter. The re-runnable commands are recorded in `DOMAINS.md` row 10 and in `FINDINGS.md`.

Also verified clean and worth not re-reviewing: every server role gate on every data-intelligence
endpoint (401/403 probed per role); the two-step IDOR guard on `GET /api/predictions/staff/:userId`;
`sql.raw` day-window interpolation (`parseInt`-fenced, injection probed); the staff attention feed's
ranking inputs (objective file state only — no predictive score, no engagement tier, so **no
disparate-service risk on the evidence**); and `PredictionInsights`' UDAAP cut, which deliberately
suppresses the odds percentage and the single funding date in favour of a qualitative label and a
week band. **That last one is recorded so a future session does not "restore" the numbers.**

### Two guards measured and found blind

- `tests/queryErrorHandling.test.ts:78` `EMPTY_DEFAULT` matches only a **literal** `= []`; the two
  commonest spellings (`x || []`, `x ?? []`) are invisible, and `:29` never scans
  `client/src/components`. Executed with the coalescing spelling substituted: **13 missed pages + 7
  missed components** against a reported baseline of **8**. F-0820-25 and F-0820-26 are both
  instances this run found by hand. **F-0820-24.**
- The reachability census in the new workflow script passes for all thirteen service functions — and
  `intent_events` holds `friction_event` and nothing else, so `intentScore` is 0 platform-wide. **A
  scored input with no ingestion path is vacuous even when its function has call sites.** Second
  occurrence of the `evaluateOutboundSms` pattern. **F-0820-30.**

### Standing findings re-surfaced

- **F-0819-03 (P1) → FIXED**, `7861794a` / PR #617, which cites the id. Re-confirmed **live** earlier
  in this same run before the fix landed (`POST /api/homeowner/profile` → 500 `TypeError:
  value.toISOString is not a function`; `POST /api/homeowner/equity` → 500 `ReportNotNullViolationError`).
  One fact this run adds: the commit's narrative attributes the throw to `""` from untouched controls,
  but this run's repro sent a **well-formed** `"2020-05-01"` and still threw — the defect was broader
  than the commit describes. The fix covers it anyway, via `z.coerce.date()` rather than by accident.
- **F-020 / F-021 / F-022 re-dated on `main`** and still live at the same lines: `mismo.ts:247`
  `mortgage: "Mortgage"`, `:251-253` `ChildSupport`/`Alimony`/`Other`, `:150` `usda: "USDA"`,
  `:216-237` bare `Other`. **43 days open — the oldest unfixed rows in the register.**
- **D-0819-04 is still live and I did not fix it**, deliberately: 19 rows whose status begins
  `**FIXED**` still sit under `## Open findings`. `FINDINGS.md` is claimed by open PR **#615**, and a
  19-row restructure of a 250-row file would have conflicted with it. I marked the one row I closed
  myself (F-0819-03) and left the rest — proposed as a ticket below.

---

## Proposed tickets — for Evening Triage to land

1. **F-0820-20 (P1)** — fix the `/api/outcomes/funnel` contract. Prefer changing the **client** to
   the server's object shape; the server's type is declared and its consumers are value-exact.
   Ship a client render test with it — there is none for this file.
2. **F-0820-21 (P1)** — `predictiveEngine.ts:235`: coerce `numeric` before `.toFixed`, or register a
   `pg` type parser. **Sweep for siblings** — `:221` returns a string on an interface typed
   `number|null`, and `data-intelligence.ts:315-317` serialises `avgConfidence: null` from the same
   cause. This is a class, not a line.
3. **F-0820-22 (P2)** — one-line fix (read `creditAdverseActions.ts`, not the shim), but the diff
   touches a §9 trigger surface, so it ships as a draft PR with a human-written security review.
   Bundle **F-014**'s two absentees (`pricingAdapter.ts`, `creditAdverseActions.ts` missing from
   `DECISION_PATH_MODULES`) and a `STATISTICAL_ENGINE_MODULES` list asserted on **both** imports and
   table references.
4. **F-0820-24 (P2)** — fix the empty-state guard before fixing its instances. Widen `EMPTY_DEFAULT`
   to the coalescing spellings, extend the scan to `client/src/components`, and re-baseline honestly
   at ~28. Fixing the guard is what stops the next batch; this is its third measured blind spot.
5. **F-0820-27 (P2)** — `optimizationEngine.ts:619` `eq(users.role,"borrower")` → `CLIENT_ROLES`, and
   reconcile the two disjoint writers of `anonymized_borrower_facts` against the one consumer that
   reads across both. **F-0820-33** shares the root cause (the same dead role literal) and should
   ride along.
6. **F-0820-23 + F-0820-32 (P2)** — one contract test per data-intelligence endpoint asserting the
   shape the client destructures. That single class of test converts F-0820-20 and all four legs of
   F-0820-23 from a browser crash into a red CI run.
7. **F-0820-31 (P2)** — pass real values into `computeNextAction` from `borrowerGraph.ts:968`. Today
   Homi tells a borrower their file is "being reviewed" while nine documents are outstanding, under a
   code comment asserting the parity it breaks.
8. **D-0819-04 (P2, carried)** — move the 19 `**FIXED**` rows out of `## Open findings`. Blocked
   today by PR #615's claim on the file; land it once #615 merges.
9. **Founder decisions** — Actions billing (⛔ 1); whether `gate` becomes a required context on
   `main`; ratify or retire `MODEL_RISK_GOVERNANCE.md` (⛔ 3); the credit-consent scope question (⛔ 4).

---

## What I could not verify, and why

- **No browser.** The Intelligence-tab crash was proved by the live payload, by rendering the real
  component under happy-dom through the **real** `AppErrorBoundary`, and by reading the boundary's
  placement — independently, by two agents. That is code-and-HTTP proof, not a journey walk; a
  browser confirmation would strengthen it and I make no rendered-pixel claim.
- **No contrast ratios, no mobile verification.** Neither was computed, so neither is claimed.
- **The ×100 segment error (F-0820-23 leg v)** is proven from the source arithmetic
  (`outcomeTracker.ts:274` returns a percent; the client multiplies again), not from an observed wrong
  number — the seed has zero funded loans, so the symptom awaits the first one.
- **Prod-side cohort state is UNVERIFIABLE from here.** The weekly `aggregate-data` cron does not run
  locally, and local-is-the-default: I did not probe prod for anything but `/api/health`.
- **F-0819-03's third leg** (`refi-alerts`) was **not** re-verified — it returned 403 on an unowned
  `profileId`, which is an ownership gate working correctly, not the defect.
- **`pnpm guard:bundle`** was not run; it measures a build, and building was out of scope.
- **Test residue, disclosed.** Verification created two `wfa+…@test.local` borrowers, two
  applications (one withdrawn), one `analytics_events` probe row, and — via a `PUT` that has no DELETE
  route — one **all-null `borrower_profiles` row** for `test-buyer` on the shared dev DB, removable
  only by direct SQL. Two admin job endpoints were invoked and both verified to have created zero rows.
  No product code was edited; the worktree is clean.
- **Reg Z: no reading was made and none was needed** — no Domain 10 module computes a finance charge,
  APR, points-and-fees or a tolerance. `docs/reg-z/`'s procurement gap blocks nothing here.
- **No Fannie/MISMO surface in Domain 10**, so no field name, enumeration, edit code or SFC was
  needed or invented, and the 403-walled job aid was not required.

STATUS: FAIL
