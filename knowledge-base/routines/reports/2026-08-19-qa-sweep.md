# Deliverable QA Sweep — 2026-08-19

STATUS: FAIL — a new P1 exists (three of them), and GitHub Actions billing has failed again, silently skipping today's ECOA adverse-action sweep.

COVERAGE: domains 9/14 reviewed · workflows 7/8 run · open P0 **0** · open P1 **27** · oldest unfixed **F-020 (42 days)**

> **The coverage line changed shape today, and the reason belongs in it.** Yesterday's line would have read `open P0 3`. It was never 3: all three P0 rows carry `**FIXED**` status cells and merely sit under the `## Open findings` heading. See **D-0819-04** — the register's own placement was overstating the launch-blocking backlog, which is exactly the hazard `routines/CHARTER.md` §1 names. P1 = 24 carried (27 minus 3 whose fixes merged 2026-08-18) + 3 new today.

**Targets and why.** Domain **9 (Compliance analytics & adverse action)** — the lowest-numbered domain never reviewed. Workflow **7 (Lifecycle / evergreen re-engagement)** — the lowest-numbered workflow never run. No tiebreak was needed; both were the first unreviewed entry in their ledger.

---

## ⛔ Human actions — hardest first

**1. GitHub Actions billing has failed again. Every CI run since ~14:22 UTC today is red, and today's ECOA adverse-action sweep did not run.**

This is the known 2026-08-18 failure class, recurring. Evidence, not inference:

```
$ gh api repos/:owner/:repo/actions/runs/32285226663/jobs
gate (typecheck · tests · schema guard)  failure  steps=0  18:04:01Z→18:04:03Z
migrate-prod (apply pending migrations)  skipped
verify-deploy (prod is serving this commit)  skipped

$ gh api .../check-runs/<id>/annotations
"The job was not started because recent account payments have failed or your
 spending limit needs to be increased. Please check the 'Billing & plans' section"
```

- **The repo is `PRIVATE`** (`gh repo view --json visibility`), so the 2026-08-18 flip-to-public workaround is not in effect.
- **This is billing, not a red `main`.** I read the annotation rather than inferring from the red — a transitive-dep CVE produces an identical all-red picture with a completely different fix.
- **Compliance consequence, and it is the reason this is item 1 rather than an ops footnote.** The `0 14 * * *` slot is `/api/jobs/adverse-action-delivery` — the ECOA 30-day watchdog (`.github/workflows/cron-jobs.yml:32`). Today's 14:22 UTC run **failed**; 08-15 → 08-18 all succeeded, so **exactly one sweep was missed, today**. The workflow's own comment explains why nobody would know: *"because this job has no failure alarm, nothing said so"* — written after three sweeps died on a DNS change on 2026-08-06. **The same blind spot has now fired twice for two unrelated reasons.**
- **Prod is NOT stranded** — `GET https://homiquity-production.up.railway.app/api/health` returns `commit: b799b91d...`, equal to `origin/main`'s tip.
- **17 open PRs cannot merge** while the gate cannot start, and `migrate-prod` is `skipped` rather than run.

**Founder actions:** (a) clear the Actions billing failure; (b) re-fire today's missed sweep — the workflow has a `workflow_dispatch` lever built for exactly this (`cron-jobs.yml:36-38`, *"re-fire a missed sweep after an outage"*); (c) decide whether a cron failure alarm is now worth building, since the no-alarm gap has cost two silent misses.

**2. `F-0819-01` — the denial chokepoint fails open, and its test pins the bug green.** A staff-created counteroffer notice satisfies the guard that is supposed to make a denial impossible without a compliant notice. A file reaches `denied` with zero denial notices **and no audit entry**. This is the control that ECOA compliance on the denial path rests on. Fix is small (scope the de-dup by `actionType`) and must land with a re-fixtured test.

**3. `F-0819-02` — a co-applicant's protected-class record can be overwritten with the primary's answers**, and separately the co-borrower is **never prompted** for demographics at all. Both halves reach the delivered package and the fair-lending join.

**4. Counsel Ask 2 has a mechanical answer for half of it.** Every adverse-action notice names the **CFPB**; Reg B Appendix A item 9 assigns the **FTC** to a non-depository originator, and the Appendix forecloses the supervisory-authority defence in its own words. The remaining half — whether Homiquity is the *creditor* whose agency belongs on the notice in a brokered transaction (§1002.9(g)) — is still counsel's. See `F-0819-04`.

**5. A schema gap blocks ECOA measurement and must not be papered over.** `F-0819-05`: the platform records **no completed-application timestamp**, so the §1002.9(a)(1)(i) deadline is not computable at all. The fix needs a column. 🚨 **Do not backfill a guessed value onto a compliance/provenance column** — a NULL is an honest gap, a wrong value is a falsified record.

---

## Summary

Domain 9 and Workflow 7 were both reviewed for the first time; the team produced 47 candidates, of which **37 entered the register** after adversarial verification, **8 were refuted** and recorded, and the rest merged into existing rows. Three P1s: the denial chokepoint fails open on any pre-existing non-denial notice (**F-0819-01**), the standalone HMDA route is borrower-sequence-blind (**F-0819-02**), and the Homeowner Hub's entire write surface returns 500 (**F-0819-03**). Verification did real work in both directions — two proposed P1s were downgraded on refuted premises, one candidate died outright as a Reality-Map false positive, and one was reframed from "untested" to the sharper "a green test pins the bug". Two long-standing escalations moved: **U-8 narrows** (all four regulatory hosts answered 200 with real rule text; the notice half of `ux-11` is now ruled a defect under Reg B §1002.13(c)) and **U-11 partially closes** for FCRA. Separately, the register's own accuracy was the third headline: **open P0 is 0, not 3**, and three P1 rows still read `open` while their fixes merged yesterday.

---

## Evidence

### Setup and honesty rails
- Clean worktree at `origin/main` @ `b799b91d`, branch `routine/qa-sweep-2026-08-19`, `node_modules` symlinked. The primary checkout is on a peer's branch (`feat/landing-coach-first`, +9 commits, 3 uncommitted files) and was never written to.
- **`:5002` is the 14-day-old orphan** — PID 20814, `ps` START Aug 5, from the deleted `launch-hygiene` worktree; `/api/health` returns `{status,timestamp}` while current code at `server/routes.ts:79-84` also returns `commit` and `email`. **It was not contacted by any agent.**
- **`:5001` was used for the Workflow 7 live run, and the caveat was discharged rather than waved:** `git diff --name-only origin/main...feat/landing-coach-first` touches **no file on this workflow's path**; the only two server/shared files in that diff are **comment-only** renames. The PID was re-verified identical at start and end of the run.
- **The UX leg booted its own server on `:5003` from the clean worktree** and drove real Chromium via `scripts/browser-probe.cjs`, so its layout numbers are measured against `origin/main`, not a peer's branch.
- Owned tests, executed: Domain 9 + lifecycle — **10 files / 111 passed**; Workflow 7 — **7 files / 94 assertions** green. Every filename appeared in the run output.

### The three P1s
- **F-0819-01** `creditAdverseActions.ts:556-561` → `getAdverseActionsByApplication` at `:360-366`, whose entire predicate is `.where(eq(adverseActions.applicationId, applicationId))` — no `actionType`. Creatable precondition: `compliance.ts:861,878` accepts `actionType: z.enum([...,"counteroffer",...])` behind `isInternalStaffRole` + deal-team. Audit write sits inside `if (aa.created)`, so the bypass is traceless. **`tests/adverseActionFcraChokepoint.test.ts:225-233` fixtures `[{ id: "aa-preexisting" }]` with no `actionType` field and asserts `{ok:true, created:false}` — 12/12 green.**
- **F-0819-02** `compliance.ts:1310-1312` and `:1348-1350`, `LIMIT 1` with no `ORDER BY`. Nondeterminism proven on a same-shape temp table: `after insert → seq=1`, `after re-saving seq1 → seq=2`. The dashboard gate does not close it — the key it lives under (`queryClient.ts:259-261`) is never invalidated by the URLA save (`URLAForm.tsx:402,427-429`), `staleTime` 5 min. `dashboard.ts:118-121` measures completeness by row presence, so the co-borrower is never asked.
- **F-0819-03** `guaranteesHomeowner.ts:134-145` / `:230-243` / `:178-190`. Nine live probes: dates-filled → 500, dates-blank → 500, keys-omitted → 201; equity with `snapshotDate` as string **and** epoch → 500 both; refi-alerts with both rates → 201. `DashboardView.tsx:50-51` mounts the two broken sections for **every** profile.

### Verification moved five findings
| candidate | proposed | final | why |
|---|---|---|---|
| breach alarm unactionable | P1 | **P2** | premise refuted — `underwriting/compliance.ts:71-99` + `ComplianceTab.tsx:98-113` is an always-on staff KPI reading the same data, pinned by a test |
| wrong federal agency | P1 | **P2** | reasons, creditor identity and FCRA attribution all still correct; misdirection is toward an agency that does take mortgage complaints |
| 30-day clock anchor | P1 | **P2** | §1002.9(a)(1)(**ii**) *does* anchor at the action for incomplete applications — the code is right for that branch; **and a second site was found in verification** (`taskEventEmitter.ts:143-151`) |
| rule-DSL prohibited-basis denylist | P3 defect | **REFUTED** | Reality-Map false-positive class: zero client callers, admin-only, context is caller-supplied — demographic data has no route in |
| AVM confidence band discarded | P2 | **REFUTED** (leg 1) | `vendors.ts:179-181` derives `confidence` and the band from hash outputs of the same address seed — consuming them would propagate simulated noise |

Also refuted and recorded: the notice's recovery-card ordering (a documented 2026-08-04 adjudication pinned by a test; the measurement used a fixture **78% longer** than any real record), the "three staff surfaces contradict" claim (three different events — one governs *lender submissions*), hand-rolled zero-states (a documented §8 backlog with a stated remediation policy), and "no per-notice ECOA deadline exists" (it does, `adverseActionDelivery.ts:148-166`).

### Compliance source reachability, re-probed today
| source | result |
|---|---|
| eCFR API (12 CFR 1002 §§2/9/12/13 + App. A; 1003 §2; 47 CFR 64.1200) | **200**, bodies grepped, 302-canonicalised — all 7 sections retrieved |
| consumerfinance.gov (Official Interpretations) | **200**, 193 KB, contains comment 9(a)(1)-1 verbatim |
| law.cornell.edu (15 U.S.C. §1681m) | **200**, §1681m(a)(1)–(4) retrieved |
| govinfo.gov `/pdf/` path | **200**, real PDF (not the HTML soft-404) |
| Fannie Mae Loan Delivery job aid | **403 — still blocked.** `docs/fannie-mae/` remains the only Fannie authority |

`CLAUDE.md`'s Fannie-403 claim is **still accurate**; its "all four regulatory hosts blocked" claim is **still stale**. Reg Z remains procurement-blocked (`docs/reg-z/` holds only a README), so `F-0819-21` is **flagged, never asserted** → **U-0819-01**, routed to `compliance-watch`.

### Cross-agent corroboration worth recording
- **F-0819-02** was found independently from opposite ends — `feature-reviewer` from the SQL, `ux-reviewer` from the missing sequence control in the UI. Merged into one id rather than two.
- **F-0819-12** was found by **three** agents independently, all landing on exactly `Pacific/Guam` and `America/Puerto_Rico` and no others. An exhaustive sweep of all 8,760 hours of 2026 gives **1,587 violating instants** — which is why their chosen instants differed. Guam is the severe leg: **1,460 h/yr at local 04:00–07:00**.
- The two verifiers **split** on `F-0819-06` (P3 vs P2). Resolved to **P2** and the disagreement is recorded in the row, because the domain verifier had two facts the compliance one did not: no set-once guard, and the borrower's page records nothing.

### Register accuracy (D-0819-04)
- Independently re-verified on `b799b91d`: **F-051** fixed by `1eb8fdb8` (#545) — `mapAusRecommendation(application.ausRecommendation)` now feeds the tag that was the literal `"Approve"`; **F-034/F-035/F-036** fixed by `02a4d8af` — `consentCoversPullType` gates by scope, both `createCreditConsent` callers pass the text actually displayed, the funnel copy is a shared constant the client renders from the same module, `isSimulated` set at INSERT.
- **F-076 (#551), F-077 (#553/554/556), F-0818-09 (#580)** — all confirmed `MERGED` 2026-08-18 via `gh pr view`, while their rows read bare `open`. Status corrected to `fix landed — awaiting domain re-verification` (not closed; re-verification is a domain re-run).
- **F-048 is genuinely open** — its only commit is the docs one that registered it.
- **F-020 re-verified still live** — `server/mismo.ts:247,251-253,255`. 42 days, the oldest unfixed row.

---

## Proposed tickets — for Evening Triage

| # | ticket | rank |
|---|---|---|
| 1 | Clear Actions billing; re-fire the missed adverse-action sweep via `workflow_dispatch`; decide on a cron failure alarm | ⛔ blocking everything — 17 PRs frozen, a compliance sweep silently skipped |
| 2 | **F-0819-01** scope `ensureAdverseActionForDenial`'s de-dup by `actionType` + re-fixture `adverseActionFcraChokepoint.test.ts` | A/B — the denial-path compliance control |
| 3 | **F-0819-02** key the `/hmda` GET+POST by `borrowerSequenceNumber`; make the dashboard completeness check per-borrower; add the missing unique constraint | A/B |
| 4 | **F-0819-03** validate `POST /api/homeowner/profile` through `insertHomeownerProfileSchema`; supply `snapshotDate` and the two rates server-side | B — the Hub's write half |
| 5 | **F-0819-04** derive the administering agency from `companyIdentity` (FTC per Appendix A item 9); founder to close counsel Ask 2's creditor half | A — every notice ever generated |
| 6 | **F-0819-05** add a completed-application timestamp; anchor both sites (`adverseActionDelivery.ts`, `taskEventEmitter.ts`). **No backfilled guesses.** | A |
| 7 | **F-0819-10** fix `NotificationsPanel`'s read-state to use `status`; re-fixture the test that hand-writes a field the API never sends | B — program-scale, every notification in the app |
| 8 | **F-0819-08** add a program filter + deterministic tiebreak to `getMarketRate30YrFixed` | B — consumer-facing savings claims |
| 9 | **F-0819-17** change the `quietHours` assertion that pins a 5 AM-Guam instant as correct, then fix `F-0819-12`'s bracket | B — before any outbound SMS ships |
| 10 | **D-0819-04** move the ten `FIXED` rows out of `## Open findings` into `## Closed` in one pass | hygiene, but it is distorting every coverage read |
| 11 | **F-0819-19 / F-0819-20** roadmap rows: no staff surface for the lifecycle module; no fair-lending monitoring over actual dispositions | roadmap |
| 12 | ⚠️ Verify **PR #605**'s "touch-target backlog 232 → 0" claim before treating it as closed — `ui-standard-guard.cjs:173-188` counts only `<Button size="sm">` and skips `components/ui`, so the 13 protected-class checkboxes at 16×16 (`ux-0819-11`) were **never in the 232** | B |

## What I could not verify, and why
1. **Any authenticated Domain 9 route end-to-end.** No authenticated session was established against a dev server for the domain leg — `:5002` is the orphan and `:5001` is a peer's checkout. `F-0819-02`'s row-flip is proven from the SQL plus an empirical temp-table reproduction, **not** from an observed wrong row in the product.
2. **Whether the lifecycle cron has ever executed in production.** The schedule and the route are verified; a prod run is not. The ledger grade says **"cron-registered (prod execution unverified)"** deliberately — claiming "prod-cron-live" without an observed run would be the green-check-is-not-a-deploy error in another costume.
3. **Reg Z on `F-0819-21`.** Procurement-blocked; flagged, not asserted (U-0819-01).
4. **Contrast ratios and real-device touch behaviour.** `browser-probe` measures neither; no AA claim is made on any surface this run.
5. **A detector gap worth recording against the tool:** `browser-probe`'s document-level overflow check **passes** on `ux-0819-06` because the overflow is contained inside `main.app-surface`. Its green is not evidence of no clipping.

## Side effects disclosed
Test entities, all fake, all on the shared local dev DB: users `wfqa+wf7@test.local`, `wfqa+wf7b@test.local`, `wfqa+verif1@test.local`; two homeowner profiles; one application; an `sms_opt_outs` row for `+15550109999`; and one for `+15551234567` from an unsigned-webhook gate probe (the documented non-production posture, `webhooks.ts:49-57`). The two admin lifecycle sweeps also wrote 4 equity snapshots, 1 refi alert and 2 notifications across pre-existing `@test.com` fixture profiles — unavoidable, since `runLifecycleSweep` has no scope parameter, and now recorded as a pre-flight step in the rewritten §7 script. No destructive SQL, no `db:push`, no product code edited.

STATUS: FAIL
