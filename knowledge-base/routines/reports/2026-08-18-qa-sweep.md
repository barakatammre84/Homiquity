# Deliverable QA Sweep — 2026-08-18

STATUS: WARN — Domain 7 and Workflow 6 both reviewed for the first time and **both fail**; the
program's only open P0 closed today; no new P0.

```
COVERAGE: domains 9/14 reviewed · workflows 6/8 run · open P0 0 · open P1 26 ·
          oldest unfixed F-020/F-021/F-022 (41 days, seeded 2026-07-08)
```

## ⛔ Human actions — hardest decision first

1. **Decide the Reg Z source rail.** `CLAUDE.md` tells every session that `ecfr.gov`,
   `consumerfinance.gov`, `govinfo.gov` and `law.cornell.edu` are *"blocked from this environment."*
   **All four return HTTP 200** (probed this run; §1026.36(e)(3)(i)(C) retrieved verbatim, 67 KB).
   That sentence is load-bearing twice over — it is the stated reason five regulatory-ledger entries
   sit unverified *and* the stated reason a Reg Z reading may never be asserted. **Two agents in this
   very run declined to rule Reg Z questions citing the blockade**, one after reading a 302
   canonicalisation redirect as a failure. **This routine did not relax the rail on the strength of
   its own finding** (routines/CHARTER §1b — a rail the machine can relax for itself is not a rail);
   every Reg Z reading recorded today stays flagged-not-asserted. **D-0818-01 (P1).**
2. **F-0818-09 must land before any lender is flipped to `approvalStatus:"approved"`.** The only
   submission UI posts the lender's uuid PK where the server resolves the business `lender_id`, so
   every submission 422s. It is masked today **only** because every lender is still `target`/`isDemo`
   — meaning **the first real submission in company history will fail**, on the day a broker
   agreement is signed. One-file rename fix; the payload already carries `lenderName`.
3. **Fannie's Loan Delivery job aid is 403 from this environment**, so `CLAUDE.md`'s compliance
   step #2 ("fetch and search it whenever you need current MISMO data point names, enumerations,
   edit codes, or SFCs") has **no executable path in-session**. Either land an export in
   `docs/fannie-mae/` or amend the instruction. **U-29** — third regime with the same shape as U-8/U-11.
4. **An unmerged orphan branch already solved one of today's findings.** `bf199bb5` *"Ratchet the
   number of buttons that do nothing"* ships `tests/inertButtons.test.ts` tracking 34 inert buttons
   and names the exact `Generate LE` button filed today as ux-0818-01.
   `git branch -a --contains bf199bb5` is **empty**; six commits of that line are stranded.
5. **Today's `lender-delivery-gate` report is absent** (12:30 slot) — CHARTER §4 WARN, named not
   ignored. Today's wiring-audit and the early evening-triage pass are present and were read.

## Summary

Reviewed **Domain 7 (AUS & lender submission)** and ran **Workflow 6 (verification-driven provenance
promotion)**, both for the first time, with six agents and four adversarial verification passes;
**25 findings entered the register and 3 were killed or merged**, including one proposed P1 that was
a duplicate of `ux-32` and one whose headline was refuted by an executed probe. **Acceptance question
A fails on four independent counts** — a borrower under two years at their job (F-052), any ARM
(F-053), any file more than three business days past the TRID trigger (ux-30), and now the lender
picker itself (F-0818-09) — three of which are permanent and none of which is an edge case.
**Workflow 6 fails because the workflow as scripted does not exist in the product**: the
three-dimension gate is real and correct, but its route has zero client callers, so the only shipped
path is a one-click override requiring no evidence at all. The program's **only open P0 (F-051)
closed today** — re-verified on `main` rather than taken from the PR — and the three oldest open P1s
were re-dated and are still live at 41 days.

## Evidence

**F-051 closed.** `server/mismo.ts:894` reads `application.ausRecommendation` through
`mapAusRecommendation` (`:194-202`); `:895` gates the whole `AUTOMATED_UNDERWRITINGS` container on a
non-null result, so an absent recommendation **omits** the container. PR #545, merge `1eb8fdb`.
Open P0 count: **0**.

**No live verification was possible, and nothing was contacted.** The `:5002` listener is a
13-day-old orphan: `lsof` → PID 20814; `ps` START `Wed Aug 5 16:08:46 2026`; cmdline points at
`.claude/worktrees/launch-hygiene`, a deleted worktree. Its `/api/health` returns
`{"status":"ok","timestamp":…}` while current code at `server/routes.ts:79-84` returns `commit` and
`email` too. **Zero bytes were sent to it.** All live legs are marked NOT VERIFIED.

**Source reachability, reconciled (two agents disagreed).**

| Host | Result |
|---|---|
| `ecfr.gov` API | **200** (a 302 adds `subpart=E`; follow it) |
| `consumerfinance.gov` | **200** |
| `govinfo.gov` | **200** |
| `law.cornell.edu` | **200** |
| `singlefamily.fanniemae.com/job-aid/loan-delivery` | **403** |

Recipe that works: `curl -sL "https://www.ecfr.gov/api/renderer/v1/content/enhanced/current/title-12?chapter=X&part=1026&section=1026.36"`.
The fetch tool also enforces a **~125-character-per-quote ceiling** — ask subparagraph-by-subparagraph.

**Domain 7 — F-0818-09, orchestrator-verified independently before any agent claim was accepted.**
`wholesale_lenders` has no `name` column (`shared/schema/lendingWholesale.ts:22-26`); the dialog
declares one (`SubmissionReadinessDialog.tsx:63-68`), renders `value={l.id}` (`:387`) and posts it as
`lenderId` (`:124`); `getWholesaleLenderByLenderId` is `WHERE lender_id = $1` with no fallback
(`server/storage/pricingPolicy.ts:408-416`). Regression: `c188c6da` (#417), 29 files, not this one.

**Workflow 6 — the gate is correct, its reachability is not.** Probed four ways: 1 dimension → no
promotion; 2 → no promotion; the *same* dimension ×3 → no promotion; the 3rd distinct → `verified`
+ stamps + `recalculateDecision(id,"verified")`. But `POST /:id/verify/:dimension` has **zero client
callers**, and commit `92d69508` (2026-07-01) added it *"instead of one all-or-nothing toggle"* — the
override it was meant to supersede is the only shipped path (F-0818-01, P1).

**Tests run.** Domain 7: 5 files / **88 passed**. Workflow 6: 16 files / **219 passed** — and
`grep -rln "services/verification" tests client/src` returns **nothing**, so none of them touches the
subject of the workflow.

**Register hygiene.** F-020/F-021/F-022 re-verified byte-unchanged on `main` and re-dated; their line
numbers had drifted +32/+2/+39 from the F-051 fix, which is exactly the D-012 rot that makes a stale
row look checked. `DOMAINS.md` §7 corrected (it named two files containing **zero** submission
surfaces, and a "Target-5 catalog" the code deleted). The Workflow 6 script was **replaced**, not
annotated — it was itself a finding (D-0818-03), the second occurrence of the D-014 pattern.

**Mid-run rebase.** `origin/main` advanced 7 commits during this run (#559–#566, including three
`ux-38` batches that rewrite the exact `<Link><Button>` patterns the UX findings cite). The branch was
rebased and **every headline finding re-verified against `46006862`** before recording. All survive
at the same lines.

## What I could not verify, and why

- **Every HTTP status, DB row and rendered pixel in this report is derived from code**, not observed.
  No dev server, no screenshots.
- **F-0818-01's production claim** (`CREDIT_VENDOR_MODE=simulation` set on Railway) rests on a
  **2026-08-06 in-repo statement** (`.env.example:73-77`), not a probe. `list-variables` was
  deliberately not run — it returns secret values, and dumping prod secrets into a transcript to
  settle a severity question is a bad trade.
- **Compliance conclusions are flagged, never asserted**, per CLAUDE.md — notwithstanding action 1.
  Selling Guide **A2-2-04** and **B3-2-01/B3-2-02** are not in `docs/fannie-mae/` (inventory verified:
  ULDD/UCD/URLA/SFC/EarlyCheck only), so F-0818-11 and F-0818-12 carry NEEDS-COMPLIANCE-AUDIT.
- **~20 candidate findings were NOT run through `finding-verifier`** and are therefore **not in the
  register** (CHARTER rule 2: unverified findings do not exist). They are queued below.

## Proposed tickets — for Evening Triage

1. **F-0818-09** — lender-picker id/name mismatch. One-file rename. **Gate on: before any lender approval.**
2. **D-0818-01** — correct `CLAUDE.md`'s blocked-hosts claim; add the working eCFR recipe and the job-aid 403.
3. **F-0818-01** — require evidence linkage on the provenance override, or wire the granular route.
4. **ux-0818-01 + ux-30** — fix in one PR: wire or remove the dead `Generate LE` button.
5. **F-0818-07** — `brokerSubmissionReadiness.ts:117` calls the LE late a day early; use `trid.ts`'s `endOfDueDay`.
6. **UX cluster ux-0818-03 + ux-0818-04 + ux-34** — one fix, one guard rule: flag
   `text-{success,warning,status-*}` not paired with a `-subtle` background. Reaches public surfaces.
7. **F-0818-10** — a test for `SubmissionReadinessDialog`; `submitToDU` has none either.
8. **D-0818-02(a)** — `CLAUDE.md:53`'s "Target-5 catalog" (outside this routine's territory).
9. **Rescue `bf199bb5`** — the inert-button ratchet, stranded with 5 sibling commits.
10. **U-29 / U-30 / U-31** — job-aid 403; per-file LO compensation under §1026.36(d)(1); A2-2-04's URL slug.

**Verification queue for the next run** (candidates found but not adversarially verified, so
deliberately excluded from the register): 10 UX candidates from the Domain 7 surfaces (submission
milestone never reaching the borrower; no AUS findings surface; lender approval status hidden in the
picker; `DialogContent` with no scroll cap; a borrower CTA to a 404 route), 3 compliance candidates,
and 5 Domain 7 P3s (`mismoPackageGeneratedAt` not matching the hashed XML's timestamp; AUS mutation
invalidating only one query key; the `mismo-package` route having no client caller).

STATUS: WARN
