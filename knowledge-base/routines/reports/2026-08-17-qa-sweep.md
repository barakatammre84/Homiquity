# Deliverable QA Sweep — 2026-08-17

STATUS: FAIL — an open **P0 (F-051)** has aged **5 days unowned**, and the reason it aged is that the
entire 2026-08-12 QA sweep register was left **uncommitted** and never reached `main`. Rescued today.

```
COVERAGE: domains 8/14 reviewed · workflows 5/8 run (workflow 3 static-only) · open P0 1 · open P1 24 · oldest unfixed F-014/F-020..F-023 (40 days, seeded 2026-07-08)
```

Coverage moved +3 domains (6 reviewed today; 8 recorded from the rescue; row 8 had read "not yet run"
while nine of its findings already existed) and +1 workflow (3, static half only).

**How the P0/P1 counts were derived, because the register cannot be counted mechanically.** I parsed
every P0/P1 row's status cell: **24 begin with `open`** (1 P0 + 23 P1), **8 begin with `FIXED`**
(including F-034/F-035, the two phantom P0s the rescue retires), and **1 is neither** — F-024, whose
cell opens `**STILL OPEN — and no longer unverifiable**`, counted as open. A first, cruder parse of
mine returned 16 P1 by matching `FIXED` anywhere in the cell, which silently swallowed rows whose
*prose* mentions a fix (ux-01's "BOTH cited evidence sites are FIXED"). **That is D-012's structural
finding reproducing on the person counting it**: one free-text status column holding two vocabularies
means no guard — and no routine — can age a row mechanically. D-012's proposed fix (a controlled
status vocabulary plus a `last-verified-against` sha per row) would have made this count a one-liner.

---

## ⛔ Human actions

1. **F-051 (P0) — the delivered MISMO package tells every wholesale lender the AUS said `Approve`.**
   Re-verified live by me on `main` @ `d1ef64e`: `server/mismo.ts:860` emits
   `AutomatedUnderwritingRecommendationDescription` as the compile-time literal `"Approve"`.
   `loanApplications.ausRecommendation` exists (`shared/schema/lendingCore.ts:50` —
   `approve_eligible|approve_ineligible|refer|refer_with_caution`) but **no recommendation field
   exists anywhere in the MISMO DTO** (`grep recommendation shared/mismo.ts` → nothing), so the
   emitter had nothing to read. A `refer_with_caution` file is delivered as `Approve`.
   Acceptance question A. **Owner needed — it has had none for 5 days.**

2. **Authorize a Reg Z capture pass into `docs/reg-z/` (U-26).** `CLAUDE.md` and
   `docs/reg-z/README.md` both state that every authoritative source is blocked from this
   environment. **That is factually stale**, disproved twice today by two agents independently:
   `consumerfinance.gov/rules-policy/regulations/1026/{2,24,Interp-2}/` → **200**, the eCFR
   *versioner API* → **200**, `law.cornell.edu` → **200**. Only eCFR **HTML** is blocked. The real
   gap is that nothing is *captured and versioned*, so a reading is unrepeatable. Consequence:
   five ledger entries sit unverified against a blocker that no longer exists, and two P1s today
   (F-076, F-079) are held below their evidence because no session may assert a Reg Z reading.
   Founder-held: this changes a binding project rule.

3. **Decide whether the borrower Loan Options page is a §1026.18 disclosure (F-076).** If it is,
   the §1026.22(a)(2) 1/8% tolerance is exceeded **4–7×** and F-076 escalates P1 → P0. Fetching
   the regulation does **not** settle this — it is a characterization question for counsel. It is
   the single highest-leverage open question in Domain 6.

4. **Do not fix F-052/F-053 in isolation.** F-080 (co-borrower dropped from the delivered package)
   is P1 *only* because those two currently block organic files from the transmission path. Fixing
   them without F-080 silently promotes a materially false loan file — misstating who earns the
   income, under the wrong SSN — into an immutable SHA-256-hashed lender submission.

5. **Do not fix F-024 by counting Saturdays alone.** Quantified over 3,652 closing dates: that fix
   flips **9 dates to the non-conservative direction** (~1/yr), where the gate would pass a CD
   delivered *late* — creating the violation it guards against, which CLAUDE.md prohibits. F-083
   records the constraint.

6. **Kill the orphan on port 5002** (PID 20814). It answers `/api/health` 200 from code dated
   **2026-08-05**, out of worktree `.claude/worktrees/launch-hygiene` which no longer exists, and
   its health payload carries **no `commit` field** — so it cannot be dated from the outside. Any
   future routine that probes it will verify 12-day-old code and report a live pass. Nothing was
   sent to it today.

---

## Summary

The headline is not a defect, it is a process failure: the 2026-08-12 QA sweep produced a full
Domain 5 + Domain 8 review, 25 findings, 6 UX findings, 4 doc-drift rows, 8 escalations and a
re-dating pass that proved 7 of 13 open P0/P1 rows overstated the code — and left all of it
**uncommitted in a launch-gate branch's working tree**, where it was invisible to every downstream
routine for five days, including an unowned P0. I verified per-ID that none of it was on
`origin/main`, transplanted it onto a clean branch, and re-dated the severe rows against current
`main` myself. Today's own pass then reviewed **Domain 6** and ran **Workflow 3**, producing four
new P1s in Domain 6 plus one in Domain 8 — three of which two agents that never saw each other's
work found **independently**, which is the strongest signal this program generates. Every finding
went through adversarial verification before entry: six verifiers ran, and they killed or corrected
sub-claims in **five of the eleven** candidates, including three of four in one UX finding. The
most consequential result is not any single defect but **D-014**: run exactly as scripted,
Workflow 3 catches 3 of the 9 registered Domain 8 findings and **misses the P0** — because every
step-3 assertion is a schema assertion and the defects are all schema-valid falsehoods.

---

## Evidence

### The rescue

- Working tree of `routine/launch-gate-2026-08-17-clean` carried uncommitted edits to
  `DOMAINS.md` + `FINDINGS.md`. Per-ID check against `origin/main`: `F-051 F-052 F-057 F-058 F-063
  F-075 ux-24 ux-29 D-009 D-012 U-18 U-25` → **all `main=0`**. No `2026-08-12-qa-sweep.md` exists.
- Transplanted onto `routine/qa-sweep-2026-08-17` (branched from `origin/main`, later rebased to
  `d1ef64e`) as commit `b39d989`, recorded verbatim as authored 2026-08-12.
- Corroborated independently by today's launch gate, which flagged that two of three 2026-08-12
  reports live only on unmerged branches.

### Re-dated by me against `main` @ `d1ef64e` (§10 — date every standing claim)

| Row | Status today | Evidence |
|---|---|---|
| F-051 (P0) | **live** | `server/mismo.ts:860` literal `"Approve"`; `shared/mismo.ts` has no recommendation field |
| F-054 (P1) | **live** | `server/mismo.ts:812` `LoanAmortizationType` literal `"Fixed"` |
| F-055 (P1) | **live** | `:185` `mapPropertyUsage → "PrimaryResidence"`; `:173` `mapLoanPurpose → "Purchase"` |
| F-020 (P1) | **live** | `:219` `mortgage: "Mortgage"` |
| F-021 (P1) | **live** | `:149` `usda: "USDA"` |
| F-034, F-035 (P0) | **FIXED** `02a4d8a` | the rescued pass had already re-dated them — **two phantom P0s come off the board** |

`mapMortgageType`, `mapLiabilityType` and `mapAssetType` all default to a *positive* value on
unknown input — the same class as F-055.

### Domain 6 — four P1s, three found twice independently

| Defect | feature-reviewer | compliance-auditor | verdict |
|---|---|---|---|
| APR is a flat spread, not Appendix J (**F-076**) | pricing-01 | Q1 | CONFIRMED |
| `/compare-offers/:id` fabricated offers (**ux-32**) | pricing-07 | Q1 | CONFIRMED → P2 |
| QM cap `not_evaluated` (**F-081**) | pricing-08 | Q3 | CONFIRMED → P2, harm refuted |
| FTHB waiver unreachable (**F-082**) | pricing-10 | bonus | CONFIRMED → P2, framing rejected |

- **F-076** `server/services/loanAnalysis.ts:138` → `apr: (rate + (loanType === "fha" ? 0.5 : 0.25))`.
  Understates **0.45–0.94pp** whenever MI is in force; slightly *over*states on a no-MI conventional
  at ≤80 LTV. The specimen that needs no legal ruling: on the `points: 1` scenario a borrower pays a
  **$3,600 discount point and the displayed APR moves 0.000pp**. The repo asserts this invariant in
  four places (`apr.ts:6-8`, invariant I5, the app-guide, a spec) and the borrower surface
  contradicts all four, while the *marketing* surface already does it correctly.
- **F-077** the LE's disclosed MI and the DTI the engine decides on both come from a hardcoded card;
  swept the live grid — the card exceeds the `CONVENTIONAL_PMI` matrix in **all 32 cells, 1.42–2.17×**.
  `underwritingEngine.ts:319-320` computes `calculatedDti` **before** the matrix lookup at `:400-411`,
  and `resolvedPmiMonthlyPremium` is grep-proven never read in any comparison. With F-087, one
  borrower sees **three different MI figures for one loan**: $239.20, $184.00 and $429.33.
- **F-078** reproduced against **production**: `GET https://www.homiquity.com/api/calculators/credit-tiers`
  → exceptional (790) **6.376** … building (620) **6.401** — **0.025pp across the whole FICO range**.
  At `÷4` it would be 0.625pp. Both `/100` lines date to the initial commit and have never been touched.
- **F-079** seven cron sweeps exist; **none touches TRID**. `mismoValidation.ts:766-820` computes
  `tridStatus{leDueDate,leIssued}` and nothing in `client/src` reads it — computed into the void.

### Workflow 3 — FAIL, and the script is the finding

- No dev server; every live leg marked `NOT VERIFIED`. Executed evidence only: **224 tests passed,
  1 skipped, 0 failed** across 6 files, plus 6 read-only probes.
- **Step 3 passes on what matters**: XSD-clean on both purposes with an empty offending baseline, and
  the SSN appears **exactly once**, at `TaxpayerIdentifierValue`, dashes stripped (`ssn-with-dashes: 0`,
  `digits-only: 1`).
- **Step 5's `[gate]` does not exist.** `routes/lending/delivery.ts:25-71` is the whole handler; no
  router-level middleware can hide one (`routes/lending/index.ts:15-27` calls `app.get()` directly).
  Empty-file probe → **3,468 bytes** asserting a 30-year fixed mortgage for a borrower who does not exist.
- **F-080** probe: `PARTY count: 1` · `FirstName ['Jane']` · `Employer FullNames ['Acme (seq 1)',
  'Globex (CO-BORROWER seq 2)']` · one `TaxpayerIdentifierValue` · `EmploymentMonthlyIncomeAmount
  ['8000.00','6500.00']` · `validateULDDCompliance {"valid":true}` · `xmllint … validates`. Authority
  is in-repo, not memory: `docs/fannie-mae/uldd-implementation-guide.pdf` p.14 — *"The PARTY container
  will also be repeated for multiple borrowers."*
- **D-014** — caught 3 of 9; **missed the P0**. Reproduced: `refer → Approve`,
  `refer_with_caution → Approve`, `approve_ineligible → Approve`, `undefined → Approve`;
  `amortizationType=adjustable → "Fixed"`.

### Adversarial verification did its job

Six verifiers; sub-claims killed or corrected in five of eleven candidates:

- **ux-31**: 3 of 4 sub-claims wrong. `denied` is in the allowlist **deliberately** — added by
  `8370779` *as the fix*; the "13 of 17" census is wrong (16/11); the `$0` case doesn't reproduce
  against 176 real rows. P2 → **P3**.
- **ux-30**: "the borrower can never retrieve their LE" is wrong (direct API works — 4 of 176 rows
  carry `le_issued_date`, all QA curl). "Every file shows a false *Deadline Passed*" is wrong on both
  words — **1 of 176** renders red and it is truthful. The **inverted** claim is the finding:
  **173/176 files render an affirmative green "TRID Compliant" badge derived from a null due date**.
- **F-081**: no fee/dollar harm (identical fee set) and no gate bypassed — P1 → P2, compliance-risk
  yes → **no**. Cited line number was wrong.
- **F-082**: "every FTHB is over-quoted" **rejected** — the waiver carries no citation, so whether it
  is owed is a compliance question (U-28). Wiring it would be *worse* than leaving it dead.
- **F-080**: the assets/liabilities third **refuted and struck** — ASSET isn't emitted under
  `loanDelivery` (Fannie ignores it), LIABILITY is correctly DEAL-level.
- **ux-34**: contrast confirmed at 2.592:1 (recomputed) but "only instance" refuted — a **second,
  worse one on a public route** (`PropertyDetail.tsx:268-279`); dark mode is fine and was dropped.

### Peer reports (§4)

Present for 2026-08-17: `launch-gate`, `vendor-procurement`. **Absent: wiring-audit,
sprint-blitz/primary-engineer, lender-delivery-gate** — named, not silently ignored. Launch gate
reports `RELEASABLE: yes · rollback ✗` and flags Railway billing (~2026-09-06) as the live deadline.

---

## Proposed tickets (for Evening Triage — not landed here)

| # | Ticket | Rank |
|---|---|---|
| 1 | **F-051** — read `ausRecommendation`; if absent or the AUS leg is simulated, **omit the `AUTOMATED_UNDERWRITINGS` container**, exactly as `mismo.ts:405-408` does for citizenship | P0 · A |
| 2 | **F-076** — route `loanAnalysis.ts:138` through `calculateMortgageAPR`; pin with a test asserting `loan_options.apr` came from the solver (F-090 proves the current test cannot) | P1 · A/B |
| 3 | **F-077** — delete `calculatePMI`; use the `pmiAnnualRate` `derivePricing` already resolves. Correct the false docstring at `loanCosts.ts:594-599` | P1 · A/B |
| 4 | **F-078** — one conversion helper for points→rate; fix both `/100` sites. Public calculator is live and borrower-adverse | P1 · B |
| 5 | **F-080** — emit one PARTY per `borrowerSequenceNumber`; filter employment by sequence. **Ship with or before F-052/F-053** | P1→P0 · A |
| 6 | **ux-30 + F-079** — give the borrower a gated route to their own LE (the server already distinguishes `isBorrower`); split the badge into "delivered on X" vs "within window"; rank the intake pool by `leDueDate` | P1 · A/B |
| 7 | **D-014** — add an "emitted == stored" leg to the Workflow 3 script **before** re-running it | P1 · A |
| 8 | **U-26** — capture Reg Z into `docs/reg-z/`, then re-run the five flagged ledger entries | founder |
| 9 | ux-32 — delete the `/compare-offers/:id` route registration until it binds to the pricing engine | P2 · B |
| 10 | F-084 — stop the export artifact asserting "lender-ready"; stamp draft/preview or 409 with blockers | P2 · A |
| 11 | ux-34 — add `text-success-foreground` at both sites; teach the token guard the fill/foreground pairing rule | P2 · B |
| 12 | D-013 — correct `DEVELOPER_PLAYBOOK.md` §2.4 (a)(b)(c). *(d), the stale Reality-Map entry, was corrected in this PR* | P2 |

**Territory note (CHARTER §6):** this routine writes findings, never fixes. No product code was
touched; the only edits are `knowledge-base/feature-review/**` and this report. No REGISTER.md claim
taken. Four peer sessions were live (`ListAgents`); none claimed these files.

STATUS: FAIL
