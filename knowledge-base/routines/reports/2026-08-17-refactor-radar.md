# Refactor Radar — 2026-08-17

STATUS: OK — full run; one behavior-preserving PR opened ([#528](https://github.com/barakatammre84/Homiquity/pull/528)) on the first verify attempt, not merged.

## ⛔ Human actions

1. **Review and merge (or reject) [#528](https://github.com/barakatammre84/Homiquity/pull/528).** It is the run's only PR and the routine never merges. ~10 minutes: read `client/src/lib/amortizationEstimate.ts` against `git show origin/main:client/src/pages/calculators/AmortizationCalculator.tsx` lines 32–171.
2. **Know that GitHub was in a Partial System Outage during this run** (`githubstatus.com` indicator `major`, 16:59 UTC). GraphQL 503'd — `gh pr create` and `gh pr list --label` were both unusable — while REST and git transport stayed up. **CI checks on #528 may therefore be missing or delayed through no fault of the branch.** If #528 shows zero check-runs, that is the Actions-outage signature, not a broken PR: re-trigger with a one-line body edit rather than assuming the change is bad, and check `autoMergeRequest` across the queue before nudging anything.

## Summary

Reconciled the ledger against `gh pr list` (RR-012 correctly `done` at merged #481; no open radar PRs, so no backpressure and full mode applied). Reused the 5-day-old research cache per the 30-day policy — no fetching, so rail R7 had nothing to constrain this run. Re-ran the size sweep and picked **RR-015 `client/src/pages/calculators/AmortizationCalculator.tsx`**: a ~140-line pure-math block inline in a page whose two sibling calculators had both already extracted theirs, on the borrower capture path (CHARTER §1 question B) and carrying no query keys at all — the highest-value-per-unit-risk candidate available. Extracted it to `client/src/lib/amortizationEstimate.ts` with colocated characterization tests, 519→378 lines, 296/400 diff budget, all gates green on attempt 1 of 5. The sweep also surfaced `client/src/pages/admin/Lenders.tsx` (685 lines, 4th-largest client file) which the 2026-08-08 seed had missed entirely — recorded as RR-016.

## Evidence

**Preflight.** `gh auth status` ✓ (`barakatammre84`). No `refactor-radar` worktree present, so no stale-run abort; the two worktrees under `.claude/worktrees/` (`pensive-noether-5232f2`, `strange-kilby-c389c0`) belong to peer sessions and were left untouched. Worktree cut from `origin/main` @ `1f520b1` (#516).

**Ledger reconciliation (Phase 0.2).** `gh pr list --label refactor-radar --state all` → both radar PRs MERGED (#481 RR-012, #472 the install). Ledger already matched; no `in-pr`→`done` or closed-unmerged corrections were owed. **Zero open radar PRs → no backpressure.**

**Register (CHARTER §5).** `ListAgents` showed one live peer (`homiquity-10`, interactive). Active claims table was empty; claimed RR-015 at 16:21 UTC in `80d693d` **before** touching code, released it in this PR. No peer claim within 24h touched this file.

**Research.** `RESEARCH.md` `Last refreshed: 2026-08-12` — 5 days old, under the 30-day threshold, so Phase 1 skipped fetching entirely. Cache remains empty: all 5 sites are recorded as blocked by the sandbox egress proxy on 2026-08-12. No new findings, no new `adopt-candidate` rows.

**Target selection.** Size sweep re-measured (URLAForm 625→**750**, PreApproval 882→**904**). Scored the capture-path candidates for UI-vs-logic mix; `AmortizationCalculator.tsx` won on risk-adjusted value:

- 4 interfaces + `defaultInputs` + `simulate` + `calculate` = lines 32–171, all module-private, **zero** other importers (`grep 'AmortizationInputs|AmortizationResults' client/src` outside the file → empty).
- **Zero** `queryKey` / `invalidateQueries` occurrences in the file → R6's key-preservation obligation is trivially satisfiable rather than merely asserted.
- Two sibling precedents to copy: `lib/affordabilityEstimate.ts` and `lib/rentVsBuyEstimate.ts` (merged RR-012 / #481).

`URLAForm.tsx` (750 lines) is the higher §1-question-A prize but carries 9 query hooks and 21 local functions — component surgery, not pure extraction. Deferred, not dismissed; it stays `open` as RR-005.

**Behavior preservation.** Two independent proofs, because "I moved it verbatim" is exactly the claim that deserves checking:

1. Mechanical extraction + `diff` against `origin/main`:
   `✅ VERBATIM: moved block is byte-identical to origin/main (modulo added 'export ')`
2. The characterization tests' expected values were generated by **executing `origin/main`'s original inline implementation** in a scratch harness outside the repo (`tsx`, canonical `paymentAtMonthlyRate` inlined from `shared/lib/amortization.ts`), *then* asserted against the new module. A drifted copy fails these rather than passing vacuously — the same trap that has caught tests-passing-against-buggy-code here before. 8 tests green **before** the page was edited; branches covered: extra-payment acceleration, 0% rate (`monthlyRate <= 0` → straight-line), partial final payment, 15-year term, floating-point payoff residue, and `simulate`'s `Infinity` guard.

Query-key grep over the diff returns empty, as expected: `git diff origin/main...HEAD | grep -E '^[+-].*(queryKey|invalidateQueries)'` → no output.

**Verification (attempt 1 of 5, all green).** `pnpm check` ✓ · `pnpm test` node lane 191 files/2714 tests ✓, client lane 72 files/517 tests ✓ · **test-ran assertion**: 8 assertions named `client/src/lib/amortizationEstimate.test.ts` in verbose output (colocated under `client/src`, glob-picked, so no `vitest.config.ts` edit was needed) · `guard:tokens` ✓ (97 literals, at baseline) · `guard:querykeys` ✓ (all three sub-guards; the 77 unreachable-fetch-key list is the pre-existing warning, not a failure) · `guard:schema` ✓ · `guard:migrations` ✓ (57, contiguous) · `guard:channel` ✓ (frozen at 1482) · `guard:kb` ✓ (135 docs) · `pnpm build` ✓.

**§9 rehearsal.** Run exactly as CI does, with the real PR body: `security-review-guard: OK — no §9 trigger among 5 changed file(s).` No security-review section was authored — R5 keeps the diff inside `client/src/**`, so the gate has nothing to fire on. (Standing caveat: a green §9 gate proves a review was written down, never that one was correct.)

**Rails.** Diff cap **296/400** production lines (+154/−142, `knowledge-base/**` and test files excluded). No off-limits path touched; `package.json` and `pnpm-lock.yaml` unchanged; `git status --porcelain` clean of anything outside the five intended files. Never merged, no auto-merge, no push to `main`.

## Proposed tickets (for Evening Triage)

1. **Review #528** — the only action this run produced.
2. **RR-005 `client/src/pages/borrower/URLAForm.tsx` (750 lines, +125 since the seed sweep)** — the largest capture-path file and the one that speaks to §1 question A. It is a *component-surgery* job (9 query hooks, 21 local functions), so it deserves a deliberate human-reviewed decomposition rather than a radar run's ≤400-line budget. Worth scoping as a Sprint Blitz item.
3. **RR-016 `client/src/pages/admin/Lenders.tsx` (685 lines)** — 4th-largest client file, absent from the 2026-08-08 seed sweep, never audited. Low borrower-path value (admin surface), so it ranks below RR-005 on CHARTER §1.
4. **GitHub outage hygiene** — if any PR opened 2026-08-17 shows zero check-runs, it is the outage, not the change. Diagnose by whether runs get *scheduled*.

## Next 3 radar candidates

| rank | id | target | why |
|---|---|---|---|
| 1 | RR-004 | `client/src/components/ScenarioSimulatorDialog.tsx` (485) | blocker cleared (#467 merged); shrank since the seed — re-audit needed |
| 2 | RR-013 | `client/src/pages/realtor-engine/ScenarioDesk.tsx` (575) | unaudited; likely inline scenario math worth routing to `shared/lib/amortization` |
| 3 | RR-010 | `client/src/pages/borrower/Tasks.tsx` (596) | capture-path adjacent; two-axis status vocabulary makes it worth a careful look |

STATUS: OK
