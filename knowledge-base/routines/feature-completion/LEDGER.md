# Feature Completion Engine — cross-run ledger

One row per run. **Rotate by oldest last-touched domain**, with the two standing priority
segments the fleet has no other owner for (complex files; the above-conforming borrower).

The `Refused` column is the load-bearing one: a gap this routine declined, and *why*, must not be
re-derived from scratch next run. A refusal with a reason is a finished piece of work.

| date | domain / area | gap taken | shipped | refused, and why | still open |
|---|---|---|---|---|---|
| 2026-08-22 | 6 — Pricing, rates & disclosures · **area 18, Offer comparison** — taken under the standing **above-conforming borrower** segment | **The offers surface said nothing at all when it could not price.** `/api/loan-applications/:id/offers` answers with four statuses and builds borrower-facing `missingItems` copy for three of them (`server/routes/lending/pricing.ts:482,510,529`); `MarketPricingSection` opened with `if (market.status !== "PRICED") return null`, and it is the endpoint's only client reader — so `missingItems` had **zero** readers app-wide and the whole "Live Market Pricing" block silently vanished. It bites the above-conforming borrower hardest: their entire market is one product (`BRC-J30`, `server/seedMarketPricing.ts:204`) with `minCreditScore: 700` and `maxLTV: 89.99`, so a jumbo file just outside that box prices to zero offers and lands on `NO_ACTIVE_RATE_SHEETS` far more readily than any conforming file. | **[PR #657](https://github.com/barakatammre84/Homiquity/pull/657)** — the three non-`PRICED` statuses now render an explained state through the shared `patterns/EmptyState`, with the server's own `missingItems` listed and a standing qualifier that the scenarios below are estimates, not offers and not an approval. Copy is honest in the one direction that matters: *"a statement about our current sheets — not a decision on your application"*. New colocated test file (the component had none); mutation-proven both directions. | **No copy that implies we can place a loan we have no lender for** — the move-up lane's M6 rail; the `NO_ACTIVE_RATE_SHEETS` text describes today's sheets and stops. **No server change**: adding a borrower message for `NO_ACTIVE_RATE_SHEETS` would be an API payload change, which DESIGN_SYSTEM §14 puts outside a UI-lane diff — filed as a ticket instead. **Did not fold in F-0818-06** (the anti-steering card is ungated on option availability, `LoanOptions.tsx:193`): it is Reg Z-flagged and changes when a borrower may acknowledge, which gates rate lock and submission — that needs its own reviewed diff, not a ride-along. **Did not touch the single-offer `LOWEST_RATE` badge** (`pricingAdapter.ts:239`), a superlative over a set of one that only the jumbo borrower ever sees — server lane. | F-0818-06 · ux-33's card-view leg · the single-offer superlative badge · a server-side reason string for `NO_ACTIVE_RATE_SHEETS` — all four written up as proposed tickets in [`reports/2026-08-22-feature-completion.md`](../reports/2026-08-22-feature-completion.md) |
| 2026-08-20 (2) | 12 — Property, listings & homeowner · **area 22, Homebuyer accelerator** (founder-directed follow-up) | **The two gaps the first run refused.** Founder answered both ⛔ items from [#632](https://github.com/barakatammre84/Homiquity/pull/632): **yes**, Homiquity staffs 1:1 sessions — **with a loan officer, and mind the word "coaching"** — and **yes**, open a door to `/accelerator`. | **[PR #641](https://github.com/barakatammre84/Homiquity/pull/641) (draft — §9)** — the request now reaches a human. A borrower may only create a `requested` session (server-decided status, Zod-validated body, past datetimes refused); the request fans out in-app to admin/lo/loa; a **1:1 requests** card in the LO Command Center lets a loan officer confirm, which puts their name on the record. Borrower copy stops calling it coaching. Two doors opened: the aspiring-owner sidebar and the RenterHome toolkit. Expand-only migration `0057` adds `assigned_to_user_id` + `confirmed_at`. | **No email or SMS leg** — that is a new outbound-messaging surface and a §9 trigger; followed `complaintEscalation.ts` (in-app only) rather than `leadNotifications.ts` (which has an email leg). **No `NotificationsPanel.tsx` href mapping** — the file is claimed by open PR #634, and an unmapped `entityType` lands on `/dashboard`, so the notification body names the destination in words instead and the mapping is a proposed ticket. **The `coaching_sessions` table and `/api/accelerator/coaching` keep their names** — renaming a table is a contract migration and the wire is not what a borrower reads. | the href mapping (blocked on #634); whether an unlicensed staffer may take these sessions is an NMLS question for `docs/nmls/`, not answerable from memory |
| 2026-08-20 | 12 — Property, listings & homeowner · **area 22, Homebuyer accelerator** (`Last reviewed: never`) | **The program's progress never moved.** Nothing in the codebase had ever written `accelerator_enrollments.current_phase` — verified with `git log -S 'currentPhase' -- server/ shared/`, which finds only the initial commit and the #193 route split. A borrower could tick all 18 seeded milestones and the header still read *Phase 1 of 6 · 17%*. The client's `MilestonesSection` already invalidated `/api/accelerator/enrollment` on every toggle, so the surface had been *expecting* this write since it was written. | [PR #632](https://github.com/barakatammre84/Homiquity/pull/632) — `shared/acceleratorProgram.ts` (new): one definition of the six phases, plus `deriveAcceleratorProgress` / `enrollmentProgressPatch`. The milestone route now recomputes the phase from the whole milestone set (derived, never incremented, so un-ticking walks it back) and completes/reopens the enrollment. `ProgramHeader` now states *n of 18 milestones complete* instead of a phase ratio that showed 17% to someone who had done nothing. Second defect closed in the same surface: the client's `PHASE_NAMES` ("Foundation", "Financial Fitness"…) had drifted from the phase names the server actually seeds as milestone categories ("Financial Assessment", "Credit Optimization"…) — the header and the milestones inside it named the same phase differently. | **1. The coaching session that books nothing.** "Schedule Session" writes a `coaching_sessions` row and toasts *"Coaching session has been scheduled."* `getCoachingSessions` has exactly one reader — the borrower's own page. No staff surface, no notification, no task. Whether Homiquity staffs 1:1 coaching is a founder call, and the rail is *never invent a service tier we do not offer* — so this is escalated, not patched. Also: `ScheduleSessionDialog.tsx` is in open PR #605, i.e. claimed. **2. Opening a door to `/accelerator`** — the program has zero entry points anywhere in the app. Deliberately left shut: wiring a link while (1) is unfixed would convert a dormant false promise into a live one. **3. `POST`/`PUT /api/accelerator/enrollment` pass `req.body` straight to storage** with no Zod and, unlike their milestone/coaching siblings, no `userId` strip — a different gap (input validation) in the same file; folding it in would have muddied a one-gap diff. | all three refusals above, as proposed tickets in [`reports/2026-08-20-feature-completion.md`](../reports/2026-08-20-feature-completion.md) |
