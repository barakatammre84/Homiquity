# Feature Completion Engine — cross-run ledger

One row per run. **Rotate by oldest last-touched domain**, with the two standing priority
segments the fleet has no other owner for (complex files; the above-conforming borrower).

The `Refused` column is the load-bearing one: a gap this routine declined, and *why*, must not be
re-derived from scratch next run. A refusal with a reason is a finished piece of work.

| date | domain / area | gap taken | shipped | refused, and why | still open |
|---|---|---|---|---|---|
| 2026-08-20 | 12 — Property, listings & homeowner · **area 22, Homebuyer accelerator** (`Last reviewed: never`) | **The program's progress never moved.** Nothing in the codebase had ever written `accelerator_enrollments.current_phase` — verified with `git log -S 'currentPhase' -- server/ shared/`, which finds only the initial commit and the #193 route split. A borrower could tick all 18 seeded milestones and the header still read *Phase 1 of 6 · 17%*. The client's `MilestonesSection` already invalidated `/api/accelerator/enrollment` on every toggle, so the surface had been *expecting* this write since it was written. | [PR #632](https://github.com/barakatammre84/Homiquity/pull/632) — `shared/acceleratorProgram.ts` (new): one definition of the six phases, plus `deriveAcceleratorProgress` / `enrollmentProgressPatch`. The milestone route now recomputes the phase from the whole milestone set (derived, never incremented, so un-ticking walks it back) and completes/reopens the enrollment. `ProgramHeader` now states *n of 18 milestones complete* instead of a phase ratio that showed 17% to someone who had done nothing. Second defect closed in the same surface: the client's `PHASE_NAMES` ("Foundation", "Financial Fitness"…) had drifted from the phase names the server actually seeds as milestone categories ("Financial Assessment", "Credit Optimization"…) — the header and the milestones inside it named the same phase differently. | **1. The coaching session that books nothing.** "Schedule Session" writes a `coaching_sessions` row and toasts *"Coaching session has been scheduled."* `getCoachingSessions` has exactly one reader — the borrower's own page. No staff surface, no notification, no task. Whether Homiquity staffs 1:1 coaching is a founder call, and the rail is *never invent a service tier we do not offer* — so this is escalated, not patched. Also: `ScheduleSessionDialog.tsx` is in open PR #605, i.e. claimed. **2. Opening a door to `/accelerator`** — the program has zero entry points anywhere in the app. Deliberately left shut: wiring a link while (1) is unfixed would convert a dormant false promise into a live one. **3. `POST`/`PUT /api/accelerator/enrollment` pass `req.body` straight to storage** with no Zod and, unlike their milestone/coaching siblings, no `userId` strip — a different gap (input validation) in the same file; folding it in would have muddied a one-gap diff. | all three refusals above, as proposed tickets in [`reports/2026-08-20-feature-completion.md`](../reports/2026-08-20-feature-completion.md) |
| 2026-08-23 | 5 — Underwriting & decisioning · **area 6, Income analysis across the five qualifying paths** (`Last reviewed: 2026-08-12`) — taken under the standing **complex files** segment | **The itemised income breakdown did not add up to the total above it, on two surfaces.** A persisted `IncomePathResult` recorded what a path was WORTH and never what it CONTRIBUTED, and the subject property's 2–4-unit qualifying rent contributed without being a path at all. Measured with a scratch harness against `computeIncomePaths`, all three with $6,000 of wages: two rentals at +1,250/−750 → rows $6,500 under a $7,250 total (the row showed the portfolio's NET while the total counted only the positive offsets, and the $750 loss moved to the debt side unmentioned); an owner-occupied duplex at $1,500 market rent → rows $6,000 under a $7,125 total, the $1,125 with **no row at all**; a single losing rental → a **−$750 row badged "Counted"** that is not in the total. A fourth: an `applicable` bank-statement path is an alternative METHOD, never summed, and showed a figure beside a total that excluded it. The borrower card is titled *"How your qualifying income was calculated"* and subtitled *"the same math your approval used"*; the LO cockpit renders the same list under the headline figure. DESIGN_SYSTEM.md §13 **agreement**. | [PR #709](https://github.com/barakatammre84/Homiquity/pull/709) — `DtiIncomePathResult` gains `appliedMonthlyIncome` / `appliedMonthlyObligation` (what the path fed into the total, and what it fed into monthly obligations), set by the orchestrator; `withSubjectPropertyRent` in `paths/rental.ts` puts the subject unit rent on the rental row it belongs to under the same B3-3.8-01 citation. **No migration** — both fields ride inside the existing `paths` jsonb, Zod-optional. `sumAppliedIncome` returns **null**, never 0, for a row that predates the split; the borrower read model reconstructs the legacy convention and accepts it **only when it reconciles to the persisted total** — verified against the 8 real evaluation rows in the local DB, all 8 recovered — otherwise it says the breakdown is unavailable rather than showing a partial one. Both render surfaces updated. Red-first proof by reintroduction on all three (orchestrator, view guard, LO pane). | **1. No DTI math changed, and none was touched** — `primaryMonthlyQualifyingIncome`, the per-property B3-3.8-01 split, and the provenance gate are all byte-identical; the three underwriting engines were not opened. What changed is what the result *states about itself*. **2. Did not add a sixth `subject_property_rent` path id.** `INCOME_PATH_IDS` is declared twice — `shared/incomePaths.ts:30` and `shared/situationProfile.ts:31`, identical today and unguarded (`CF-0818-01`) — so a new id silently diverges the two, and it is also a persisted `varchar` and a Zod enum. The rent rides the rental row instead. **3. Did not persist `primaryBreakdown` as a column.** It would have been the tidier model and it needs a migration; the jsonb carried it for free. **4. Did not touch `capital_gains_present`** — classified and reconciled but computed by no path, and `docs/fannie-mae/` holds no B3-3.1-09. Procurement first (`CF-0818-02`), still open. | `CF-0818-01` (the duplicate `INCOME_PATH_IDS`, unguarded) and `CF-0818-02` (capital gains classified but never computed) both still open. Evaluations persisted before the split now itemise **only** when the legacy convention reconciles — a complex file written before this PR shows the honest "breakdown unavailable" line until its next re-evaluation. |
