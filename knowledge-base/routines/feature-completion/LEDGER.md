# Feature Completion Engine — cross-run ledger

One row per run. **Rotate by oldest last-touched domain**, with the two standing priority
segments the fleet has no other owner for (complex files; the above-conforming borrower).

The `Refused` column is the load-bearing one: a gap this routine declined, and *why*, must not be
re-derived from scratch next run. A refusal with a reason is a finished piece of work.

| date | domain / area | gap taken | shipped | refused, and why | still open |
|---|---|---|---|---|---|
| 2026-08-20 | 12 — Property, listings & homeowner · **area 22, Homebuyer accelerator** (`Last reviewed: never`) | **The program's progress never moved.** Nothing in the codebase had ever written `accelerator_enrollments.current_phase` — verified with `git log -S 'currentPhase' -- server/ shared/`, which finds only the initial commit and the #193 route split. A borrower could tick all 18 seeded milestones and the header still read *Phase 1 of 6 · 17%*. The client's `MilestonesSection` already invalidated `/api/accelerator/enrollment` on every toggle, so the surface had been *expecting* this write since it was written. | [PR #632](https://github.com/barakatammre84/Homiquity/pull/632) — `shared/acceleratorProgram.ts` (new): one definition of the six phases, plus `deriveAcceleratorProgress` / `enrollmentProgressPatch`. The milestone route now recomputes the phase from the whole milestone set (derived, never incremented, so un-ticking walks it back) and completes/reopens the enrollment. `ProgramHeader` now states *n of 18 milestones complete* instead of a phase ratio that showed 17% to someone who had done nothing. Second defect closed in the same surface: the client's `PHASE_NAMES` ("Foundation", "Financial Fitness"…) had drifted from the phase names the server actually seeds as milestone categories ("Financial Assessment", "Credit Optimization"…) — the header and the milestones inside it named the same phase differently. | **1. The coaching session that books nothing.** "Schedule Session" writes a `coaching_sessions` row and toasts *"Coaching session has been scheduled."* `getCoachingSessions` has exactly one reader — the borrower's own page. No staff surface, no notification, no task. Whether Homiquity staffs 1:1 coaching is a founder call, and the rail is *never invent a service tier we do not offer* — so this is escalated, not patched. Also: `ScheduleSessionDialog.tsx` is in open PR #605, i.e. claimed. **2. Opening a door to `/accelerator`** — the program has zero entry points anywhere in the app. Deliberately left shut: wiring a link while (1) is unfixed would convert a dormant false promise into a live one. **3. `POST`/`PUT /api/accelerator/enrollment` pass `req.body` straight to storage** with no Zod and, unlike their milestone/coaching siblings, no `userId` strip — a different gap (input validation) in the same file; folding it in would have muddied a one-gap diff. | all three refusals above, as proposed tickets in [`reports/2026-08-20-feature-completion.md`](../reports/2026-08-20-feature-completion.md) |
| 2026-08-22 (2nd tick) | 11 — Staff, partner & pipeline ops · **area 17, Pre-approval & pre-qualification letters** (`Last reviewed: never`) | **An expired or revoked letter still read "ready to download", and there was no path to a fresh one.** `shared/letters.ts effectiveLetterStatus` computes expiry at read time so the API can never report `issued` on a lapsed letter, and both status endpoints return that `status`; the **staff** card renders the full Issued/Expired/Revoked/Superseded vocabulary from it. All three **borrower** surfaces read `hasLetter` alone — their `useQuery` generics did not even model `status`. Dated: the lifecycle landed `a83b3fd2`/`0746f8e8` (both 2026-08-04, #340/#343) and never reached a borrower surface; the `hasLetter` read is original. Two consequences: an expired letter is offered for download forever with **no self-serve reissue** (`hasLetter` stays true, so Generate never returns), and a revoked one 409s into a generic *"Failed to download letter."* toast. | [PR #689](https://github.com/barakatammre84/Homiquity/pull/689) — `client/src/lib/letterStatus.ts` (new): one selector, five states, three questions (download? reissue? what to say instead), consumed by `PreQualLetterCard`, `PreApprovedCard` and `LoanLetterButton`. Expired → "Get an updated letter" (the path that already worked — both generate routes INSERT a new row and every read takes `ORDER BY createdAt DESC LIMIT 1`). Revoked/superseded/draft/**unrecognised** → neither download nor reissue (fail closed). Query error → claims nothing instead of falling through to "Generate". Client-only; no server, schema or regulated math. Mutation-proven: restore the `hasLetter`-alone rule → **9 red** across 3 files. | **1. The revocation hole on `generate-letter`** — the route has status, licensed-state and provenance gates but **no revocation check**, and `loCommandCenter/ActionsRail.tsx:35` posts to it unconditionally, so a one-click staff "Generate Letter" silently supersedes another staff member's revocation. Server-side, `hq-letters-owner`'s lane, a different gap — folding it in would have made a one-gap diff into two. **Left closed on the client side deliberately**: this PR does not give the borrower a reissue button on a revoked letter, so nothing regresses while it is open. **2. Renaming pre-approval vs pre-qualification** — `PreApprovedCard` renders at `pre_approved` but queries the *prequal* endpoints, documented in the file as the deferred compliance-gated COPY track. Not touched; a regulated artefact is not renamed by a routine. **3. No `FINDINGS.md` id minted** — the defect was never in the register (219 open before, 219 after); F-061 is a different axis and stays open. | the three refusals above, as proposed tickets in [`reports/2026-08-22-feature-completion.md`](../reports/2026-08-22-feature-completion.md); plus `preQualificationLetters` allowing a `revoked` status no route can write, and the letter-expiry cron's real cadence (**UNVERIFIED** — not probed this run) |
